# llm-wikis v0.1.0 Independent Acceptance Checklist

Author: independent checklist session `/root/task01_checklist_author`  
Authored: 2026-07-29, before production implementation  
Author role: checklist author only; this session has no production implementation or final-verification role  
Authoritative specification: `docs/superpowers/specs/2026-07-28-llm-wikis-external-query-design.md`  
Authoritative plan: `docs/superpowers/plans/2026-07-28-llm-wikis-query-cli.md`

The immutable fields are ID, requirement, platform, phase, command or inspection, and expected result. The main/orchestrating session and all implementation workers may not edit, weaken, remove, or replace those fields. They may not change this checklist's requirements or expected results.

All rows begin unresolved. Only a second independent verifier who is not this checklist author and did not implement the feature may update Status and Evidence. Each `PASS` must cite a sanitized evidence artifact containing the exact command or inspection, exit status, platform, and relevant output. An unavailable check remains `PENDING`; a demonstrated failure remains `FAIL`. Material checklist defects require user-approved correction and a recorded replacement baseline hash.

The canonical immutable fields are frozen in `docs/verification/llm-wikis-v0.1.0-checklist-baseline.json`. Its raw SHA-256 and row count are recorded in `docs/verification/llm-wikis-execution.md`.

Row-addressable Rust commands are normative acceptance-harness requirements: implementation must define the exact named test in the named integration-test target already assigned by the implementation plan. The verifier runs the command literally and records its exit status and output. A closed inspection procedure is executable only when every step, input path, comparison, and pass/fail condition is enumerated in that row.

## Offline checks

| ID | Requirement | Platform | Phase | Exact command or inspection | Expected result | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| OFF-001 | The acceptance checklist was authored before production code by an independent session with no implementation role, and final verification is reserved for a different independent verifier. | All | offline | Closed procedure: (1) read the checklist Author/Authored/Author role lines; (2) read all baseline approval timestamps in execution.md; (3) read the earliest production-file commit or SHA manifest timestamp; (4) read the Task 17 verifier identity; (5) compare identities and chronology. | Author is exactly `/root/task01_checklist_author`; latest approved checklist baseline predates the earliest production change; Task 17 verifier is neither this author nor any recorded implementer. | PENDING |  |
| OFF-002 | The frozen baseline remains byte-for-byte unchanged unless the user approved a replacement. | All | offline | Closed procedure: (1) run `Get-FileHash -Algorithm SHA256 docs/verification/llm-wikis-v0.1.0-checklist-baseline.json`; (2) parse baseline `row_count` and `schema_version`; (3) parse the machine-readable active-baseline block in execution.md; (4) compare all three values; (5) verify every superseded hash/count and approval remains chronological. | Raw hash, row count, and schema exactly equal the active-baseline block; all older baselines remain recorded with explicit approvals for each replacement; any mismatch or unapproved replacement fails. | PENDING |  |
| OFF-003 | The product is one Rust package named `llm-wikis` at version 0.1.0 and needs no Python runtime. | All | offline | Run `cargo metadata --no-deps --format-version 1`; inspect Cargo targets and runtime code for Python invocation or embedding. | Metadata shows one `llm-wikis` 0.1.0 native binary, and production execution has no Python dependency. | PENDING |  |
| OFF-004 | The public CLI exposes only version, config init, list, doctor, and single-wiki query with the specified global and command options. | All | offline | Run `cargo test --test cli_contract off_004 -- --exact`. Fixture/assertion scope: Run `cargo run -- --help`; run each subcommand with `--help`; inspect the parsed command model. | Test exits 0 after asserting the only commands are config init, list, doctor, and query plus top-level version; global options are only config/json/help/version; no raw provider-argument option exists. | PENDING |  |
| OFF-005 | MCP, orchestration, multi-wiki fan-out, direct retrieval APIs, answer saving, operation logging, query-time index regeneration, wizard, self-update, uninstaller, and package-manager distribution remain out of scope. | All | offline | Run `cargo test --test cli_contract off_005 -- --exact`. Fixture/assertion scope: Run `rg -n -e "orchestration" -e "mcp server" -e "search_wiki" -e "read_wiki_page" -e "self-update" -e "uninstall" -e "homebrew" -e "winget" -e "scoop" src Cargo.toml config.example.toml install.ps1 install.sh docs/llm-wikis.md`; inspect any matches. | Test exits 0 only when no callable source/config/Cargo/installer implementation exists for any listed capability and every docs match is explicitly marked out of scope or future work. | PENDING |  |
| OFF-006 | `--version` prints the exact product name and Cargo package version. | All | offline | Run `cargo test --test cli_contract off_006 -- --exact`. Fixture/assertion scope: Run `cargo run -- --version`. | Stdout is exactly `llm-wikis 0.1.0` followed by one line ending and exit is 0. | PENDING |  |
| OFF-007 | `config init` creates missing parent directories and a valid non-interactive starter registry with runtime/provider defaults, one reusable `wiki-query/v1` profile, zero wikis, and a safe commented absolute-path example. | All | offline | Run `cargo test --test cli_contract off_007 -- --exact`. Fixture/assertion scope: Run the compiled CLI with `--config` set to a nonexistent nested temporary absolute path and `config init`; then run JSON list against it and inspect the TOML. | Parent and file are created without prompts; config parses; list returns an empty array; defaults/profile/commented operator-owned path example are present and do not imply repository-relative installation. | PENDING |  |
| OFF-008 | `config init` never overwrites or merges an existing file. | All | offline | Run `cargo test --test cli_contract off_008 -- --exact`. Fixture/assertion scope: Create and hash a test-owned canonical absolute sentinel path, invoke JSON config init against that exact path, then hash it again. | Exit is 2 with `CONFIG_EXISTS`, `created` is false, and file bytes are unchanged. | PENDING |  |
| OFF-009 | JSON config initialization uses the exact success fields and the same operation with a public error object on failure. | All | offline | Run `cargo test --test cli_contract off_009 -- --exact`. Fixture/assertion scope: Execute successful and existing-file `--json config init` cases and parse stdout as a single JSON value. | Success contains exactly schema version 1.0, ok true, operation `config_init`, absolute path, and created true; failure has operation `config_init`, created false, and the normalized error. | PENDING |  |
| OFF-010 | Query accepts exactly one allowlisted wiki ID and rejects a missing ID, repeated `--wiki`, and literal `all`. | All | offline | Run `cargo test --test cli_contract off_010 -- --exact`. Fixture/assertion scope: Run CLI contract tests and direct JSON invocations for missing, repeated, and `all` wiki selectors. | Each invalid form exits 2 with `ARGUMENT_INVALID`; no provider starts. | PENDING |  |
| OFF-011 | Query accepts either a positional question after `--` or the complete UTF-8 question from stdin when the positional form is absent. | All | offline | Run `cargo test --test cli_contract off_011 -- --exact`. Fixture/assertion scope: Run CLI contract tests with positional, Traditional Chinese, multiline, leading-dash, and stdin-only questions. | Each form reaches the service as the exact original UTF-8 bytes without truncation or reinterpretation. | PENDING |  |
| OFF-012 | Query rejects simultaneous positional and stdin questions and rejects missing or empty question input rather than merging ambiguity. | All | offline | Run `cargo test --test cli_contract off_012 -- --exact`. Fixture/assertion scope: Pipe non-empty stdin while also supplying a positional question; separately invoke with no positional content and empty stdin. | Each invocation exits 2 with `ARGUMENT_INVALID` before provider startup. | PENDING |  |
| OFF-013 | Omitting `--agent` is allowed only when the global default provider is enabled for the selected wiki. | All | offline | Run `cargo test --test cli_contract off_013 -- --exact`. Fixture/assertion scope: Run table-driven CLI tests for enabled default, absent default, and default not enabled by the wiki. | Enabled default is selected; the other cases fail deterministically without starting a provider. | PENDING |  |
| OFF-014 | Human query output prints answer, then gaps, then warnings. | All | offline | Run `cargo test --test cli_contract off_014 -- --exact`. Fixture/assertion scope: Run a fixture-backed successful query without `--json` whose result contains all three categories. | Stdout ordering is answer, gaps, warnings; diagnostics are not mixed into stdout. | PENDING |  |
| OFF-015 | JSON mode emits exactly one normalized JSON document on stdout for successes, parse errors, and runtime failures; diagnostics go to stderr. | All | offline | Run `cargo test --test cli_contract off_015 -- --exact`. Fixture/assertion scope: Run representative success, CLI parse failure, and provider failure with `--json`; parse stdout once and assert EOF; capture stderr separately. | Each stdout contains one JSON value and nothing else; stderr contains only bounded diagnostics. | PENDING |  |
| OFF-016 | `list` loads valid configuration and lists all configured wikis without resolving or starting provider executables. | All | offline | Run `cargo test --test list`; inspect its fake process runner call count. | Zero-wiki config returns a successful empty array; configured entries are returned; provider call count is zero. | PENDING |  |
| OFF-017 | List output contains wiki ID, title, enabled agents, and a derived nullable default agent. | All | offline | Run `cargo test --test list off_017 -- --exact`. Fixture/assertion scope: Execute JSON list fixtures where the global default is enabled, disabled for one wiki, and absent. | The configured default appears only when enabled; otherwise JSON `null` is emitted. | PENDING |  |
| OFF-018 | Static doctor checks every configured wiki/provider pair by default and `--wiki` and `--agent` narrow the matrix. | All | offline | Run `cargo test --test doctor` matrix-selection cases and inspect JSON result keys. | There is exactly one result per selected pair; static mode makes no paid model request. | PENDING |  |
| OFF-019 | `doctor --live` consumes quota only when explicitly requested and requires both one wiki and one agent selector. | All | offline | Run `cargo test --test doctor off_019 -- --exact`. Fixture/assertion scope: Invoke `doctor --live` with neither selector, each selector alone, and both together using a fake provider. | Incomplete selection exits 2 with `ARGUMENT_INVALID` and zero paid calls; the fully selected case invokes exactly one live verification. | PENDING |  |
| OFF-020 | Default configuration locations are platform-native and match the specification. | Windows, Linux/WSL, macOS | offline | Run `cargo test --test doctor off_020 -- --exact`. Fixture/assertion scope: Run platform-path unit tests with isolated APPDATA, XDG_CONFIG_HOME, and home-directory inputs. | Paths resolve to `%APPDATA%\llm-wikis\config.toml`, XDG or `~/.config/llm-wikis/config.toml`, and `~/Library/Application Support/llm-wikis/config.toml` respectively. | PENDING |  |
| OFF-021 | `--config` accepts only an absolute trusted path and configuration discovery never walks the caller's current project. | All | offline | Run `cargo test --test doctor off_021 -- --exact`. Fixture/assertion scope: Run relative-override rejection tests; launch the binary from an unrelated temporary cwd with an explicit config; place a decoy config in cwd. | Relative override fails with `ARGUMENT_INVALID`; explicit absolute config is used; cwd decoy is ignored. | PENDING |  |
| OFF-022 | List and doctor command-level argument or configuration failures use one top-level public error and empty result arrays. | All | offline | Run `cargo test --test doctor off_022 -- --exact`. Fixture/assertion scope: Run JSON list and doctor with malformed config and invalid selectors. | `ok` is false; list has empty `wikis`, doctor has empty `results`; exactly one normalized top-level error is present. | PENDING |  |
| OFF-023 | Doctor keeps pair-specific failures in result checks, completes static checks before live work, and applies precedence 70, 7, 6, 5, 4, 3, 2, 0. | All | offline | Run `cargo test --test doctor off_023 -- --exact`. Fixture/assertion scope: Run table-driven mixed doctor failure-class tests with an instrumented fake provider. | All selected static checks complete first; pair failures remain in checks; process exit uses the first present precedence class. | PENDING |  |
| OFF-024 | TOML deserialization is strict: `config_version` is required and equals 1, and unknown or mistyped keys fail. | All | offline | Run `cargo test --test config_contract off_024 -- --exact`. Fixture/assertion scope: Run configuration tests for missing/wrong version, malformed TOML, unknown keys at every table level, and mistyped values. | Every invalid file returns `CONFIG_INVALID`; no unknown value is silently accepted. | PENDING |  |
| OFF-025 | Optional configuration tables and fields receive only the documented defaults. | All | offline | Run `cargo test --test config_contract off_025 -- --exact`. Fixture/assertion scope: Parse configs omitting default_agent, runtime fields, providers, profiles, and wikis; inspect the resolved model. | Runtime fields independently default to 180, 65536, 1048576, 65536, and warn; optional maps are empty; provider executable defaults only when its provider table exists. | PENDING |  |
| OFF-026 | Question and stream byte limits are positive integers. | All | offline | Run `cargo test --test config_contract off_026 -- --exact`. Fixture/assertion scope: Run config tests with zero, negative, non-integer, and valid positive values for all three byte limits. | Invalid values return `CONFIG_INVALID`; positive values are preserved exactly. | PENDING |  |
| OFF-027 | Complete stdin UTF-8 validation and exact `max_question_bytes` enforcement occur inside Query Service before provider startup. | All | offline | Run `cargo test --test config_contract off_027 -- --exact`. Fixture/assertion scope: Run service-boundary tests with invalid UTF-8, exact-boundary multibyte UTF-8, and one-byte overflow; inspect runner calls. | Test exits 0 after asserting invalid input is `QUESTION_INVALID_UTF8`, exact boundary reaches the injected provider once, one-byte overflow is `QUESTION_TOO_LARGE`, and invalid/overflow provider-call counts are zero. | PENDING |  |
| OFF-028 | A wiki requires title, project_root, content_root, query_profile, and a non-empty unique agents array. | All | offline | Run `cargo test --test config_contract off_028 -- --exact`. Fixture/assertion scope: Run one strict-config fixture per missing field, empty agents, duplicate agents, and a complete valid wiki. | Invalid cases return `CONFIG_INVALID`; the complete wiki resolves. | PENDING |  |
| OFF-029 | An enabled provider requires a global provider table and a corresponding table in the referenced profile. | All | offline | Run `cargo test --test config_contract off_029 -- --exact`. Fixture/assertion scope: Run resolver tests with each table absent. | Missing global provider is `PROVIDER_CONFIG_MISSING`; missing profile provider is `PROVIDER_PROFILE_MISSING`. | PENDING |  |
| OFF-030 | Missing profiles and unsupported contracts fail with their stable codes. | All | offline | Run `cargo test --test config_contract off_030 -- --exact`. Fixture/assertion scope: Run resolver tests for a missing referenced profile and a contract other than `wiki-query/v1`. | Results are `QUERY_PROFILE_NOT_FOUND` and `CONTRACT_UNSUPPORTED`, both exit 2. | PENDING |  |
| OFF-031 | project_root and content_root resolve relative to the configuration file, not process cwd. | All | offline | Run `cargo test --test config_contract off_031 -- --exact`. Fixture/assertion scope: Use a relative-root fixture config and invoke from two unrelated working directories. | Both runs resolve the same canonical roots relative to the config directory. | PENDING |  |
| OFF-032 | content_root must be a real directory canonically contained by project_root. | All | offline | Run `cargo test --test config_contract off_032 -- --exact`. Fixture/assertion scope: Run path tests for contained directory, missing path, regular file, and canonical path outside project_root. | Test exits 0 after asserting contained directory resolves, missing path and regular-file root each return `WIKI_INVALID`, and canonical outside path returns `PATH_OUTSIDE_ALLOWED_ROOT`; all failures start zero providers. | PENDING |  |
| OFF-033 | Project-skill paths resolve under project_root; Claude local plugin_dir resolves from config and its skill_path resolves under plugin_dir. | All | offline | Run `cargo test --test config_contract off_033 -- --exact`. Fixture/assertion scope: Run resolution tests for valid relative paths and attempts to escape each declared root. | Valid artifacts resolve canonically; escapes return `PATH_OUTSIDE_ALLOWED_ROOT`. | PENDING |  |
| OFF-034 | Every configured path component and every entry in content_root and the selected skill/plugin artifact tree rejects symlinks, junctions, reparse points, mounts, and special entries. | Windows, Linux/WSL, macOS | offline | Run `cargo test --test config_contract off_034 -- --exact`. Fixture/assertion scope: Run platform path fixtures containing each available special entry at root components and recursively under protected trees. | Every encountered special entry returns `UNSAFE_FILESYSTEM_ENTRY`, even when its target remains contained. | PENDING |  |
| OFF-035 | Special-entry scans do not recursively reject unrelated portions of project_root. | All | offline | Run `cargo test --test config_contract off_035 -- --exact`. Fixture/assertion scope: Place a special entry in an unrelated project_root subtree outside content_root and the selected artifact tree; run preflight. | Preflight does not scan or reject the unrelated subtree. | PENDING |  |
| OFF-036 | Canonical regular paths outside their declared roots return `PATH_OUTSIDE_ALLOWED_ROOT`, distinct from special-entry errors. | All | offline | Run `cargo test --test config_contract off_036 -- --exact`. Fixture/assertion scope: Run canonical traversal fixtures without special entries and corresponding symlink fixtures. | Regular escape maps to `PATH_OUTSIDE_ALLOWED_ROOT`; special path maps to `UNSAFE_FILESYSTEM_ENTRY`. | PENDING |  |
| OFF-037 | Wiki IDs match lowercase kebab syntax, are unique, and query callers can select only registered IDs rather than paths. | All | offline | Run `cargo test --test config_contract off_037 -- --exact`. Fixture/assertion scope: Run config and CLI tests for valid IDs, uppercase, separators, duplicates, an unregistered ID, and a path-like selector. | Only `[a-z0-9]+` components joined by single hyphens pass; unregistered/path input is `WIKI_NOT_ALLOWED` or argument rejection without path access. | PENDING |  |
| OFF-038 | Provider executable configuration is one command name or one absolute path and rejects arguments, shell syntax, controls, and relative path components. | All | offline | Run `cargo test --test config_contract off_038 -- --exact`. Fixture/assertion scope: Run executable-value tests with command names, absolute paths, spaces as arguments, metacharacters, newlines, and relative components. | Only one safe command name or one absolute path passes validation. | PENDING |  |
| OFF-039 | Provider command names resolve through current PATH and Windows PATHEXT, and doctor reports the canonical selected path and recorded version. | Windows, Linux/WSL, macOS | offline | Run `cargo test --test config_contract off_039 -- --exact`. Fixture/assertion scope: Run isolated executable-resolution tests and JSON static doctor against fixture executables. | Resolution is deterministic and doctor exposes only the canonical executable path and version, not unrelated environment values. | PENDING |  |
| OFF-040 | Supported load modes are Claude project_skill, Claude local_plugin, and Codex project_skill only. | All | offline | Run `cargo test --test config_contract off_040 -- --exact`. Fixture/assertion scope: Parse valid fixtures for each supported mode and invalid Codex installed-plugin or unknown load modes. | The three supported forms resolve; Codex namespaced installed plugin and unknown modes fail closed. | PENDING |  |
| OFF-041 | Normal query is disabled until a current live probe matches machine, executable/version, roots, profile, entrypoint, configuration identity, and artifact fingerprint; query never performs a live probe implicitly. | All | offline | Run `cargo test --test config_contract off_041 -- --exact`. Fixture/assertion scope: Run query-service probe-gate tests for no, current, and mismatched records with a paid-call counter. | Only the exact current record allows invocation; all other cases return `ENTRYPOINT_UNVERIFIED`; query publishes no probe and never probes live. | PENDING |  |
| OFF-042 | Entrypoints accept exact Claude `/name`, Claude `/plugin-name:skill-name`, and unnamespaced Codex `$name` grammars with ASCII letters, digits, dots, underscores, and hyphens. | All | offline | Run `cargo test --test config_contract off_042 -- --exact`. Fixture/assertion scope: Run table-driven entrypoint tests containing all accepted characters and valid boundaries. | Valid forms pass without rewriting or inferred namespaces. | PENDING |  |
| OFF-043 | Entrypoints reject bad prefixes, wrong colon counts, Codex namespaces, whitespace, newlines, controls, quotes, shell metacharacters, and multiple tokens. | All | offline | Run `cargo test --test config_contract off_043 -- --exact`. Fixture/assertion scope: Run table-driven invalid entrypoint fixtures. | Every invalid value returns `ENTRYPOINT_INVALID` before provider startup. | PENDING |  |
| OFF-044 | The wrapper uses the configured entrypoint exactly and never appends namespaces, renames it, or infers it from skill_path. | All | offline | Run `cargo test --test config_contract off_044 -- --exact`. Fixture/assertion scope: Inspect prompt-builder tests with differently named skills and deliberately unrelated skill_path names. | The first prompt token exactly equals configured entrypoint in every case. | PENDING |  |
| OFF-045 | Unknown configuration rejects raw provider arguments, shell commands, prompts, tool lists, sandbox, and MCP fields; provider safety flags and prompt envelope remain implementation-owned. | All | offline | Run `cargo test --test config_contract off_045 -- --exact`. Fixture/assertion scope: Parse fixtures containing each forbidden field named in specification Section 6.4. | Every fixture returns `CONFIG_INVALID`; none reaches provider argv or prompt construction. | PENDING |  |
| OFF-046 | The project-owned overlay, not the upstream dependency, owns external-readonly behavior while preserving the upstream interactive workflow. | All | offline | Run `cargo test --test skill_contract off_046 -- --exact`. Fixture/assertion scope: Inspect `agents/overrides/wiki-query/SKILL.md`, upstream skill, and repository changes; run overlay contract tests. | Overlay has an explicit early external-readonly branch, interactive behavior remains, and upstream dependency files are not modified or claimed as owned. | PENDING |  |
| OFF-047 | Deterministic Windows and POSIX deployment scripts copy the overlay byte-for-byte to both Claude and Codex project-skill destinations. | Windows, Linux/WSL, macOS | offline | Run `cargo test --test skill_contract off_047 -- --exact`. Fixture/assertion scope: Invoke `agents/bin/deploy-wiki-query-overlay.ps1` and `agents/bin/deploy-wiki-query-overlay.sh` in isolated copies twice, then hash overlay and both destinations after each run. | Both deployed copies exactly equal the overlay; repeated deployment is deterministic. | PENDING |  |
| OFF-048 | An APM refresh or any selected skill/plugin artifact change invalidates the probe and cannot silently restore write-capable behavior. | All | offline | Run `cargo test --test probes off_048 -- --exact`. Fixture/assertion scope: Establish a valid probe fixture, mutate or roll back the artifact, then run normal query without another live doctor. | Query fails `ENTRYPOINT_UNVERIFIED` before provider invocation until the changed state passes live doctor. | PENDING |  |
| OFF-049 | The prompt starts with the exact configured entrypoint followed by the fixed EXTERNAL_QUERY JSON envelope and no configurable template content. | All | offline | Run `cargo test --test skill_contract off_049 -- --exact`. Fixture/assertion scope: Run prompt-builder golden tests for both providers and all load modes. | Prompt contains exact contract, external-readonly mode, selected wiki ID, canonical content root, and question fields in wrapper-serialized JSON. | PENDING |  |
| OFF-050 | The question is untrusted JSON data rather than interpolated prompt or shell syntax. | All | offline | Run `cargo test --test skill_contract off_050 -- --exact`. Fixture/assertion scope: Run adversarial prompt tests using quotes, backticks, `$()`, JSON-looking text, ampersand, redirection characters, newlines, and Traditional Chinese. | Decoding the envelope recovers the exact question string; no question bytes alter entrypoint, provider argv, or envelope structure. | PENDING |  |
| OFF-051 | External-readonly skill behavior uses the explicit content_root instead of repository guesses. | All | offline | Run `cargo test --test skill_contract off_051 -- --exact`. Fixture/assertion scope: Run the overlay contract against distinct temporary project_root and contained content_root fixtures. | Reads and citations come only from the explicit configured content root. | PENDING |  |
| OFF-052 | External-readonly mode reads SCHEMA.md, link-style configuration, the complete index, relevant pages, and one relevant level of cross-references. | All | offline | Run `cargo test --test skill_contract off_052 -- --exact`. Fixture/assertion scope: Execute fixture skill contract tests with required facts distributed across those sources and instrumented read evidence. | The normalized answer demonstrates all required read stages and one cross-reference level. | PENDING |  |
| OFF-053 | External-readonly answers treat wiki content as source of truth and do not fill gaps from general knowledge. | All | offline | Run `cargo test --test skill_contract off_053 -- --exact`. Fixture/assertion scope: Run synthetic fixtures containing an explicit disagreement with common knowledge and a missing fact. | Answer reports the wiki-grounded disagreement and represents missing material as gaps or follow-up questions without invented facts. | PENDING |  |
| OFF-054 | External-readonly results include grounded answer content, native inline page citations, disagreements, gaps, and follow-up questions when applicable. | All | offline | Run `cargo test --test skill_contract off_054 -- --exact`. Fixture/assertion scope: Run overlay behavior fixtures exercising each output category and validate the model result schema. | Applicable content is returned in normalized fields with native page citations. | PENDING |  |
| OFF-055 | External-readonly behavior omits answer-saving offers, never regenerates the index, and never writes pages, logs, reports, commits, caches, or other files. | All | offline | Run `cargo test --test skill_contract off_055 -- --exact`. Fixture/assertion scope: Run overlay contract tests with before/after full-tree snapshots and inspect the answer text. | Snapshots are identical and no saving offer or mutation instruction appears. | PENDING |  |
| OFF-056 | The existing skill contains an explicit early external-readonly mode branch rather than a late prompt contradiction, while interactive invocation remains available. | All | offline | Run `cargo test --test skill_contract off_056 -- --exact`. Fixture/assertion scope: Inspect overlay control flow and run external and interactive fixture invocations. | External mode enters the non-mutating branch before interactive mutation instructions; interactive behavior is preserved. | PENDING |  |
| OFF-057 | Model results require exact contract, enumerated knowledge_status, non-empty answer, and string arrays for citations, gaps, and warnings. | All | offline | Run `cargo test --test model_contract off_057 -- --exact`. Fixture/assertion scope: Run model-validation table tests with each field missing, mistyped, empty, or unknown status and one fully valid object. | Only the exact `wiki-query/v1` structure is accepted; invalid objects return `CONTRACT_VIOLATION`. | PENDING |  |
| OFF-058 | A grounded model result requires at least one page citation that resolves after deterministic normalization. | All | offline | Run `cargo test --test model_contract off_058 -- --exact`. Fixture/assertion scope: Validate grounded fixtures with no citations, only ignored drive-by references, valid inline citation, and valid returned-array citation. | Citationless or drive-by-only grounded results fail `CONTRACT_VIOLATION`; either valid page-provenance source passes. | PENDING |  |
| OFF-059 | A no_relevant_material result requires an empty citation array and at least one non-empty gap. | All | offline | Run `cargo test --test model_contract off_059 -- --exact`. Fixture/assertion scope: Run validation cases with citations, no gaps, empty gaps, and the valid combination. | Only empty citations plus a non-empty gap pass; all other combinations are `CONTRACT_VIOLATION`. | PENDING |  |
| OFF-060 | Provider-native metadata never substitutes for the required model result object. | All | offline | Run `cargo test --test model_contract off_060 -- --exact`. Fixture/assertion scope: Feed Claude/Codex fixtures containing successful native metadata but no valid model result. | Test exits 0 after asserting both syntactically valid native envelopes without a model object return `CONTRACT_VIOLATION`, exit 6, and no success envelope. | PENDING |  |
| OFF-061 | The internal Query Service has bounded config loader, wiki resolver, preflight, provider adapter, and result normalizer boundaries and is provider-neutral. | All | offline | Run `cargo test --test query_service off_061 -- --exact`. Fixture/assertion scope: Inspect `src/lib.rs`, service modules, and dependency-injected integration tests. | Each responsibility is isolated; provider-specific parsing and argv remain behind adapters. | PENDING |  |
| OFF-062 | CLI and future trusted callers use Query Service validation directly; Query Service does not print, parse CLI arguments, publish probes, or bypass UTF-8 and size checks. | All | offline | Run `cargo test --test query_service off_062 -- --exact`. Fixture/assertion scope: Inspect QueryService public API and service-boundary tests that call it without CLI. | Service enforces question/config/query rules itself and has no terminal, clap, probe-publication, or direct process-creation side effects. | PENDING |  |
| OFF-063 | Preflight requires SCHEMA.md, supported or default link style, contained rules file when named, wiki/index.md, wiki/pages, and statically addressable selected skill/plugin artifacts. | All | offline | Run `cargo test --test query_service off_063 -- --exact`. Fixture/assertion scope: Run one preflight fixture with each required artifact missing or invalid and one complete fixture. | Test exits 0 after asserting missing SCHEMA/index/pages/rules file is `WIKI_INVALID`, unsupported style is `LINK_STYLE_UNSUPPORTED`, escaping rules path is `PATH_OUTSIDE_ALLOWED_ROOT`, invalid selected artifact is `ENTRYPOINT_INVALID`, complete fixture reaches the injected provider once, and all defect cases reach it zero times. | PENDING |  |
| OFF-064 | Preflight resolves the provider executable and runs bounded version and non-billable authentication readiness before paid invocation. | All | offline | Run `cargo test --test query_service off_064 -- --exact`. Fixture/assertion scope: Use an instrumented fake runner and inspect call order for a normal query. | Version and auth probes occur before provider invocation; their bounds and errors are enforced. | PENDING |  |
| OFF-065 | Artifact fingerprint and exact current live-probe verification occur before prompt invocation. | All | offline | Run `cargo test --test query_service off_065 -- --exact`. Fixture/assertion scope: Run query-service ordering tests with missing and mismatched probes and an invocation counter. | Query returns `ENTRYPOINT_UNVERIFIED` and the paid invocation counter remains zero. | PENDING |  |
| OFF-066 | Query flow snapshots before invocation, parses and validates output and citations, always resnapshots afterward, rejects mutation, namespaces citations, and then emits one envelope. | All | offline | Run `cargo test --test query_service off_066 -- --exact`. Fixture/assertion scope: Run instrumented query-flow tests recording stage order for success and each failure class. | Observed stage order matches specification Section 8.1 and integrity comparison precedes any returned provider content. | PENDING |  |
| OFF-067 | Exactly one registered wiki and one enabled provider are resolved for each query. | All | offline | Run `cargo test --test query_service off_067 -- --exact`. Fixture/assertion scope: Run resolver tests for registered/enabled pair, unregistered wiki, and disabled provider. | Valid pair resolves once; failures are `WIKI_NOT_ALLOWED` or `AGENT_UNSUPPORTED` before provider startup. | PENDING |  |
| OFF-068 | The public command remains provider-neutral and current `/wiki-query` and `$wiki-query` names are configuration rather than constants. | All | offline | Run `cargo test --test query_service off_068 -- --exact`. Fixture/assertion scope: Run differently named profile tests and inspect CLI/service source for hard-coded current entrypoints outside examples/tests. | Compatible differently named project skills work by configuration only; provider choice does not change the public query contract. | PENDING |  |
| OFF-069 | External query never invokes `bin/generate-index.py` or otherwise regenerates the index. | All | offline | Run `cargo test --test wiki_preflight off_069 -- --exact`. Fixture/assertion scope: Run query fixtures with an executable sentinel index generator and inspect process calls and content snapshots. | Generator is never called and index bytes remain unchanged. | PENDING |  |
| OFF-070 | A missing wiki index is `WIKI_INVALID`. | All | offline | Run `cargo test --test wiki_preflight off_070 -- --exact`. Fixture/assertion scope: Run preflight with valid schema/pages but no `wiki/index.md`. | Query and doctor fail before provider startup with `WIKI_INVALID`, exit 2. | PENDING |  |
| OFF-071 | Any non-excluded page newer than the index makes the index stale. | All | offline | Run `cargo test --test wiki_preflight off_071 -- --exact`. Fixture/assertion scope: Set controlled page/index modification times with one page newer and run freshness checks. | Freshness reports stale independent of index text equality. | PENDING |  |
| OFF-072 | Freshness parses only exact generated-list shapes and ignores incidental links on other lines. | All | offline | Run `cargo test --test wiki_preflight off_072 -- --exact`. Fixture/assertion scope: Run freshness fixtures containing both exact Obsidian/Markdown generated lines and misleading incidental links. | Only exact generated-list entries contribute to the index-entry set. | PENDING |  |
| OFF-073 | Freshness excludes current-schema `audit-*.md` pages and requires exact equality between remaining regular page stems and index entries, detecting missing, duplicate, extra, and case mismatch. | All | offline | Run `cargo test --test wiki_preflight off_073 -- --exact`. Fixture/assertion scope: Run table-driven freshness fixtures for excluded audit pages and each set mismatch. | Audit pages are ignored; every other mismatch is stale even when timestamps are equal. | PENDING |  |
| OFF-074 | Freshness policy warn continues with `INDEX_MAY_BE_STALE`; policy error stops with `INDEX_STALE`. | All | offline | Run `cargo test --test wiki_preflight off_074 -- --exact`. Fixture/assertion scope: Execute the same stale fixture under warn and error policies. | Warn produces a wrapper warning and continues; error exits 2 before provider invocation. | PENDING |  |
| OFF-075 | Freshness behaves conservatively with equal and coarse-resolution timestamps. | All | offline | Run `cargo test --test wiki_preflight off_075 -- --exact`. Fixture/assertion scope: Run timestamp fixtures with equal times and coarse-rounded times plus entry-set mismatches. | Equal/coarse time alone does not falsely fail; mechanical entry-set mismatch still reports stale. | PENDING |  |
| OFF-076 | Query-provenance page citations are distinct from formal wiki footnotes to raw files, assets, or HTTP resources. | All | offline | Run `cargo test --test citations off_076 -- --exact`. Fixture/assertion scope: Run citation fixtures containing formal footnotes and page provenance together and separately. | Test exits 0 after asserting only page provenance appears in public citations; raw/assets/HTTP footnote text remains unchanged in answer prose, never appears in public citations, and alone yields `CONTRACT_VIOLATION`. | PENDING |  |
| OFF-077 | link_style is read from SCHEMA.md and defaults to `obsidian` only when the field is absent. | All | offline | Run `cargo test --test citations off_077 -- --exact`. Fixture/assertion scope: Parse schema fixtures with absent, obsidian, markdown, and unsupported values. | Absent and explicit obsidian select Obsidian parser; markdown selects Markdown parser; unsupported is rejected. | PENDING |  |
| OFF-078 | The Obsidian parser recognizes only exact `[[slug]]` with a complete lowercase safe slug. | All | offline | Run `cargo test --test citations off_078 -- --exact`. Fixture/assertion scope: Run extractor table tests for exact links, labels, whitespace, uppercase, anchors, paths, and malformed links. | Only exact safe slug form is extracted; other inline prose is ignored. | PENDING |  |
| OFF-079 | The Markdown parser recognizes exact `[[slug]]` and exact `[[slug](pages/slug.md)]` only when both slug occurrences are safe and identical. | All | offline | Run `cargo test --test citations off_079 -- --exact`. Fixture/assertion scope: Run extractor tests for both valid forms and mismatched, unsafe, case-varied, or malformed forms. | Only the two exact matching forms are extracted. | PENDING |  |
| OFF-080 | Inline raw, assets, HTTP links, ordinary Markdown links, labels, and malformed wiki-link prose are ignored rather than emitted or marked invalid. | All | offline | Run `cargo test --test citations off_080 -- --exact`. Fixture/assertion scope: Feed every excluded inline form to both built-in parsers. | Extracted citation list remains empty and no `CITATION_INVALID` is raised solely for prose. | PENDING |  |
| OFF-081 | Inline slugs preserve answer order, returned-array slugs append in array order, and the combined list deduplicates by first occurrence. | All | offline | Run `cargo test --test citations off_081 -- --exact`. Fixture/assertion scope: Run normalization with repeated interleaved inline and array slugs. | Output order is deterministic first occurrence across inline then array sources. | PENDING |  |
| OFF-082 | One outer link delimiter may be removed from returned-array citation candidates before safe-slug validation. | All | offline | Run `cargo test --test citations off_082 -- --exact`. Fixture/assertion scope: Validate returned-array fixtures using a bare slug, one supported outer delimiter, nested delimiters, and malformed delimiters. | Bare and exactly one accepted outer wrapper normalize to the slug; nested/malformed candidates fail safely. | PENDING |  |
| OFF-083 | Explicit model citation elements containing separators, traversal, anchors, URLs, or unsafe characters fail fast with `CITATION_INVALID`. | All | offline | Run `cargo test --test citations off_083 -- --exact`. Fixture/assertion scope: Run one returned-array fixture for each unsafe category alongside a later valid citation. | Each result is discarded completely with `CITATION_INVALID`; later valid items do not salvage it. | PENDING |  |
| OFF-084 | Safe slugs resolve only to exact-case regular `.md` files directly under wiki/pages. | Windows, Linux/WSL, macOS | offline | Run `cargo test --test citations off_084 -- --exact`. Fixture/assertion scope: Run resolution tests for exact file, missing file, directory, nested file, wrong suffix, and case mismatch including Windows. | Only exact ASCII filename-stem match to a direct regular `.md` file passes; failures are `CITATION_NOT_FOUND`. | PENDING |  |
| OFF-085 | SCHEMA cross-reference parsing requires one exact level-two heading and at most one exact link_style and link_style_rules field with strict syntax. | All | offline | Run `cargo test --test citations off_085 -- --exact`. Fixture/assertion scope: Parse fixtures with missing/duplicate headings, indented fields, duplicate fields, trailing prose, invalid values, and a valid subsection ending at the next level-two heading. | Invalid schemas are `WIKI_INVALID`; exact fields parse deterministically. | PENDING |  |
| OFF-086 | link_style_rules is a slash-separated relative path contained by content_root and names a regular `.md` file; its contents are not executed or parsed as configuration. | All | offline | Run `cargo test --test citations off_086 -- --exact`. Fixture/assertion scope: Run rules-path fixtures for valid file, absolute/traversal path, escape, missing/non-regular/non-md file, and malicious-looking rule contents. | Only contained regular `.md` path passes; content remains inert documentation. | PENDING |  |
| OFF-087 | Any non-built-in link style returns `LINK_STYLE_UNSUPPORTED`. | All | offline | Run `cargo test --test citations off_087 -- --exact`. Fixture/assertion scope: Run preflight with a syntactically valid but unsupported link_style. | Preflight fails before provider startup with `LINK_STYLE_UNSUPPORTED`, exit 2. | PENDING |  |
| OFF-088 | A grounded result supported only by raw, asset, or HTTP drive-by targets fails `CONTRACT_VIOLATION`. | All | offline | Run `cargo test --test citations off_088 -- --exact`. Fixture/assertion scope: Normalize a grounded result with those targets in prose and no valid page citation. | Complete answer is rejected with `CONTRACT_VIOLATION`. | PENDING |  |
| OFF-089 | Citation failures discard the complete answer; status is never inferred from prose. | All | offline | Run `cargo test --test citations off_089 -- --exact`. Fixture/assertion scope: Run normalization for invalid citation, missing citation, citationless grounded prose, and cited no_relevant_material prose. | Stable citation or contract error is returned, answer is null in public failure, and prose does not change status. | PENDING |  |
| OFF-090 | Public citation objects use the wrapper-selected wiki ID and never trust a model-supplied namespace. | All | offline | Run `cargo test --test citations off_090 -- --exact`. Fixture/assertion scope: Feed a model result containing namespace-like prose or extra metadata and normalize under a different selected wiki ID. | Every public citation contains only selected wiki ID plus validated slug. | PENDING |  |
| OFF-091 | Read-only enforcement is the mechanical roll-up of the independently verified allowlist, containment, transport, provider-policy, no-generator, limit, and snapshot controls. | All | offline | Closed procedure: (1) parse this checklist; (2) require PASS with non-empty evidence for OFF-010, OFF-021, OFF-032, OFF-034, OFF-037, OFF-038, OFF-045, OFF-050, OFF-055, OFF-069, OFF-092, OFF-093, OFF-094, OFF-095, OFF-096, PROC-001, PROC-003, PROC-005, PROC-006, PROC-007, PROC-010, PROC-015, PROC-017, PROC-020, and PROC-022; (3) fail on any absent, PENDING, or FAIL prerequisite. | PASS only when all 25 closed prerequisite rows are PASS with evidence; otherwise OFF-091 remains PENDING or FAIL and cannot self-certify read-only enforcement. | PENDING |  |
| OFF-092 | The integrity snapshot covers every directory and regular file beneath canonical content_root, including schema, config, helpers, hooks, raw/assets, index/log/overview, and pages. | All | offline | Run `cargo test --test mutation_snapshot off_092 -- --exact`. Fixture/assertion scope: Build a fixture containing each subtree category, take a snapshot, and inspect normalized entries. | Every directory and regular file appears; no content-root subtree is excluded. | PENDING |  |
| OFF-093 | Snapshots record normalized path and type plus byte length and streaming SHA-256 for files, detecting additions, removals, type changes, and same-size rewrites with restored timestamps. | All | offline | Run `cargo test --test mutation_snapshot off_093 -- --exact`. Fixture/assertion scope: Run snapshot tests for each mutation type including preserved timestamp and same length. | Each change produces a non-equal snapshot and identifies its relative path. | PENDING |  |
| OFF-094 | Snapshot traversal rejects every symlink, junction, reparse point, mount, and other special entry. | Windows, Linux/WSL, macOS | offline | Run `cargo test --test mutation_snapshot off_094 -- --exact`. Fixture/assertion scope: Run snapshot fixtures containing each platform-available special entry. | Snapshot aborts with `UNSAFE_FILESYSTEM_ENTRY` before provider content can be returned. | PENDING |  |
| OFF-095 | The after-snapshot runs in guaranteed cleanup after success, provider failure, timeout, and stdout or stderr overflow. | All | offline | Run `cargo test --test query_service off_095 -- --exact`. Fixture/assertion scope: Run query tests that mutate content during each outcome and record snapshot calls. | After-snapshot runs for every outcome and mutation is detected. | PENDING |  |
| OFF-096 | `READ_ONLY_VIOLATION` exit 7 dominates simultaneous exit 2 through 6 failures and preserves only displaced sanitized code/message; inability to complete integrity comparison is `INTERNAL_ERROR` exit 70. | All | offline | Run `cargo test --test query_service off_096 -- --exact`. Fixture/assertion scope: Run table-driven failure-precedence tests for every non-internal failure plus mutation and for an after-snapshot internal failure. | Mutation cases return exit 7 with secondary error; integrity-comparison failure returns exit 70. | PENDING |  |
| OFF-097 | Claude local plugins declaring hooks, MCP servers, settings, or other executable lifecycle components fail closed; only the configured query skill is allowed. | All | offline | Run `cargo test --test doctor off_097 -- --exact`. Fixture/assertion scope: Run static doctor against plugin manifests with each forbidden component and a minimal valid identity manifest. | Forbidden manifests fail before provider startup; minimal non-executable plugin passes static addressability. | PENDING |  |
| OFF-098 | Query success JSON matches the normalized public envelope including wiki metadata, agent, contract, status, answer, citations, gaps, warnings, duration, child exit, and native raw format. | All | offline | Run `cargo test --test output_contract off_098 -- --exact`. Fixture/assertion scope: Run JSON golden tests for grounded and no_relevant_material successes. | Output fields and types exactly match schema version 1.0 public contract with no unknown fields. | PENDING |  |
| OFF-099 | Query failure JSON uses null unresolved wiki/agent fields for early argument failures and normalized empty/null result fields elsewhere. | All | offline | Run `cargo test --test output_contract off_099 -- --exact`. Fixture/assertion scope: Run golden tests for pre-resolution argument failure and post-resolution provider failure. | Test exits 0 after asserting early failure has wiki/agent/knowledge_status/answer/raw_format null and empty citations/gaps/warnings; post-resolution failure retains wiki/agent and has knowledge_status/answer null with empty citations/gaps. | PENDING |  |
| OFF-100 | Public warnings are closed source/code/message objects with wrapper warnings first in deterministic order and provider strings normalized afterward in model order. | All | offline | Run `cargo test --test output_contract off_100 -- --exact`. Fixture/assertion scope: Normalize a result containing freshness, read-scope, and multiple provider warnings. | Wrapper warnings retain stable codes/order; provider warnings become source provider and code `PROVIDER_WARNING`; no unknown fields appear. | PENDING |  |
| OFF-101 | raw_format is `claude-json` only after parsed Claude output, `codex-jsonl` only after parsed Codex events, and null before a native format is established. | All | offline | Run `cargo test --test output_contract off_101 -- --exact`. Fixture/assertion scope: Run public-envelope tests for valid native outputs and failures before/during parsing. | Exact enumerated value or null is emitted according to parsing stage. | PENDING |  |
| OFF-102 | Every public error has required string code/message and an optional closed details object. | All | offline | Run `cargo test --test output_contract off_102 -- --exact`. Fixture/assertion scope: Serialize every AppError variant and schema-validate the JSON. | Required fields are always present, details contain only error-specific allowed keys, and no internal diagnostics leak. | PENDING |  |
| OFF-103 | OUTPUT_TOO_LARGE details identify stdout or stderr and report non-negative limit and observed bytes with observed greater than limit. | All | offline | Run `cargo test --test output_contract off_103 -- --exact`. Fixture/assertion scope: Trigger one-byte overflow independently on both streams and parse public details. | Stream is exact, counts are correct integers, and observed bytes exceed limit. | PENDING |  |
| OFF-104 | READ_ONLY_VIOLATION details contain sorted unique non-empty relative changed paths without content, absolute paths, traversal, or platform-native separators and optional one-level sanitized secondary error. | Windows, Linux/WSL, macOS | offline | Run `cargo test --test output_contract off_104 -- --exact`. Fixture/assertion scope: Mutate multiple paths in unsorted order while inducing a timeout; parse failure JSON on every supported platform; inject a Windows-backslash serialization case. | Paths are sorted, unique, relative, content-free, and always slash-separated with `/`; a changed_paths value containing `\` is rejected even on Windows; secondary error has only code/message and no nested details. | PENDING |  |
| OFF-105 | Every error code in specification Section 14 maps to its exact exit class and exit 0 means success. | All | offline | Run `cargo test --test model_contract off_105 -- --exact`. Fixture/assertion scope: Run the table-driven error-contract test covering all documented codes and compare against Section 14. | Test exits 0 after comparing every Section 14 code to its literal 2, 3, 4, 5, 6, 7, or 70 value and success to 0; no code is absent or maps to another value. | PENDING |  |
| OFF-106 | Wrapper process exit equals the documented class and failed `--json` invocations still emit exactly one envelope without panic or traceback. | All | offline | Run `cargo test --test cli_contract off_106 -- --exact`. Fixture/assertion scope: Execute one representative public CLI failure from every exit class and capture process status/stdout/stderr. | Process status matches mapping; stdout is one failure envelope; no panic, traceback, or second document appears. | PENDING |  |
| OFF-107 | Query and command-level doctor share the same stable error-to-exit mapping. | All | offline | Run `cargo test --test output_contract off_107 -- --exact`. Fixture/assertion scope: Induce equivalent config, CLI, auth, timeout, native-output, contract, mutation, and internal failures through query and doctor. | Matching errors produce matching codes and process exits in both commands. | PENDING |  |
| OFF-108 | Diagnostics are bounded and sanitized: no full prompt, wiki prose, token, session/account identifier, or absolute protected path is exposed. | All | offline | Run `cargo test --test output_contract off_108 -- --exact`. Fixture/assertion scope: Run adversarial provider stderr/native-error fixtures containing sentinel secrets and long protected data; inspect public warnings, errors, and stderr. | Test exits 0 after asserting every public/stderr field is within its configured byte cap, all sentinel prompt/prose/token/account/session/absolute-path strings are absent, and only slash-relative changed_paths are retained. | PENDING |  |
| OFF-109 | Static doctor checks config, roots, wiki structure, profile, entrypoint, executable/version, auth readiness, index freshness, and read scope using only the closed check-name vocabulary. | All | offline | Run `cargo test --test doctor off_109 -- --exact`. Fixture/assertion scope: Run JSON static doctor on fixtures that pass and fail each check; schema-validate names, status, and code. | Names are only config, roots, wiki_structure, profile, entrypoint, executable, auth, index_freshness, read_scope, live_contract, or mutation; status is pass/warn/fail. | PENDING |  |
| OFF-110 | Doctor check codes are null for pass, stable warning code for warn, or a Section 14 error code for fail; overall ok is false if any check fails. | All | offline | Run `cargo test --test doctor off_110 -- --exact`. Fixture/assertion scope: Run doctor schema tests with pass, warning-only, and failure matrices. | Code/status/overall-ok relationships match the closed contract. | PENDING |  |
| OFF-111 | Static doctor and every query emit exact Claude or Codex broad-read warning text and recommend an OS sandbox/container, never a Codex user permission profile. | All | offline | Run `cargo test --test doctor off_111 -- --exact`. Fixture/assertion scope: Run fixture doctor/query for each provider and compare warning code/message golden values. | Claude and Codex warnings exactly match specification Sections 10.2 and 10.3 and use `read_scope` in doctor. | PENDING |  |
| OFF-112 | Live doctor sends exactly the fixed two-sentence verification question from plan Task 12, validates contract/native/normalized output and unchanged snapshots, then records success outside the wiki. | All | offline | Run `cargo test --test doctor off_112 -- --exact`. Fixture/assertion scope: Capture provider stdin in live-doctor end-to-end tests and compare its question bytes to `Verify external-readonly mode by reporting one fact from the index with a valid\nwiki citation. If the wiki has no pages, return no_relevant_material with a gap.`; also exercise each validation failure. | The question contains exactly the specified two sentences with one LF after `valid` and no added prose; probe publication happens only after valid citations/contract and identical snapshots; failures publish nothing. | PENDING |  |
| OFF-113 | Probe-store paths are platform-native cache paths outside every configured wiki. | Windows, Linux/WSL, macOS | offline | Run `cargo test --test probes off_113 -- --exact`. Fixture/assertion scope: Run cache-path unit tests with isolated LOCALAPPDATA, XDG_CACHE_HOME, and home inputs plus roots that would collide. | Paths match `%LOCALAPPDATA%\llm-wikis\probes-v1.json`, XDG or `~/.cache/llm-wikis/probes-v1.json`, and `~/Library/Caches/llm-wikis/probes-v1.json`; collisions fail before provider startup. | PENDING |  |
| OFF-114 | Artifact fingerprints cover sorted normalized directories/files and use path, NUL, big-endian u64 length, and raw bytes with lowercase `sha256:` encoding. | All | offline | Run `cargo test --test probes off_114 -- --exact`. Fixture/assertion scope: Run fingerprint golden vectors with reordered creation, empty directories, binary files, and known expected digest. | Test exits 0 after asserting reordered creation equals the literal golden digest embedded in the test, each path/type/byte mutation differs, and every stored value matches lowercase `sha256:` plus 64 hex digits. | PENDING |  |
| OFF-115 | Claude local-plugin fingerprint also includes `.claude-plugin/plugin.json` and rejects special entries or executable lifecycle components. | All | offline | Run `cargo test --test probes off_115 -- --exact`. Fixture/assertion scope: Fingerprint valid plugin fixtures, mutate only the manifest, add special entries, and add forbidden manifest components. | Manifest-only changes alter fingerprint; unsafe or executable plugin content fails closed. | PENDING |  |
| OFF-116 | Probe logical key is exactly canonical roots, provider, profile, load mode, and entrypoint and permits zero or one record. | All | offline | Run `cargo test --test probes off_116 -- --exact`. Fixture/assertion scope: Run probe-reader tests with distinct keys, duplicate same-key records, and reordered file records. | Distinct keys do not collide; duplicate logical keys are unverified; one exact key can verify. | PENDING |  |
| OFF-117 | Current verification additionally matches canonical executable path/version, contract, skill fingerprint, and normalized compatibility fingerprint. | All | offline | Run `cargo test --test probes off_117 -- --exact`. Fixture/assertion scope: Establish one valid record and vary each tuple field independently. | Every current-verification change returns `ENTRYPOINT_UNVERIFIED`. | PENDING |  |
| OFF-118 | Compatibility fingerprint is canonical JSON over selected wiki, profile, executable declaration, and implementation safety/contract version; timeout, limits, freshness, comments, and TOML key order do not invalidate it. | All | offline | Run `cargo test --test probes off_118 -- --exact`. Fixture/assertion scope: Run compatibility-fingerprint golden tests varying included and excluded fields. | Included semantic changes alter digest; excluded execution/presentation changes do not. | PENDING |  |
| OFF-119 | Successful live doctor atomically replaces all historical records for the logical key with exactly one current record and failed live doctor preserves the existing store. | All | offline | Run `cargo test --test probes off_119 -- --exact`. Fixture/assertion scope: Run probe-publication tests with historical records and injected failure before rename. | Success leaves one current record; failure leaves original bytes unchanged; temporary replacement is atomic. | PENDING |  |
| OFF-120 | Query only reads probe cache; missing, malformed, duplicate, or mismatched records are unverified and records have no time-to-live in version 1. | All | offline | Run `cargo test --test probes off_120 -- --exact`. Fixture/assertion scope: Run query gate tests for each record defect plus arbitrarily old but otherwise current verified_at. | Defects return `ENTRYPOINT_UNVERIFIED`; age alone does not invalidate; query does not modify cache bytes. | PENDING |  |
| OFF-121 | Probe writes request user-only permissions where supported and never store prompts, answers, or wiki content. | Windows, Linux/WSL, macOS | offline | Run `cargo test --test probes off_121 -- --exact`. Fixture/assertion scope: Inspect serialized probe fixture and platform permission tests after live publication. | Test exits 0 after asserting serialized keys equal OFF-142's closed field list, prompt/answer/wiki-content sentinels are absent, Unix mode is user-only where supported, and Windows ACL request excludes non-user write access. | PENDING |  |
| OFF-122 | List and doctor JSON schemas exactly match schema version 1.0 operation-specific public envelopes. | All | offline | Run `cargo test --test doctor off_122 -- --exact`. Fixture/assertion scope: Run list/doctor golden-output tests for success, warning, pair failure, and command-level failure. | Test exits 0 after exact golden comparison: list success has schema_version/ok/operation/wikis; doctor success has schema_version/ok/operation/live/results; command failure adds error and uses an empty operation array; no unknown key appears. | PENDING |  |
| OFF-123 | Offline Rust quality gates pass with the separately built process helper configured. | All | offline | Run `cargo fmt --check`; run `cargo clippy --all-targets --all-features -- -D warnings`; build `tests/fixtures/process-helper/Cargo.toml`; set `LLM_WIKIS_PROCESS_HELPER` to its binary; run `cargo test --all-targets --all-features`. | Every command exits 0; missing process-helper configuration fails rather than skips platform process tests. | PENDING |  |
| OFF-124 | Existing upstream wiki helper tests pass and both deployed skill copies remain byte-identical to the project overlay. | All | offline | Run `pwsh -NoProfile -Command "$env:PYTHONUTF8='1'; python -m unittest discover -s agents/apm_modules/kfchou/wiki-skills/tests -v; if ($LASTEXITCODE) { exit $LASTEXITCODE }; $h=Get-FileHash -Algorithm SHA256 agents/overrides/wiki-query/SKILL.md,agents/.claude/skills/wiki-query/SKILL.md,agents/.agents/skills/wiki-query/SKILL.md; if (($h.Hash \| Select-Object -Unique).Count -ne 1) { exit 1 }"`. | Command exits 0; unittest reports zero failures/errors and the three SHA-256 values are identical. | PENDING |  |
| OFF-125 | Operator documentation covers installation, configuration, commands, stdin/JSON/exit contracts, read-only layers, snapshots, freshness, probes, quota, read-scope limits, overlays, and troubleshooting. | All | offline | Closed procedure: (1) run `rg -n -e "Installation" -e "Configuration" -e "config init" -e "doctor --live" -e "stdin" -e "schema_version" -e "exit" -e "read-only" -e "snapshot" -e "fresh" -e "probe" -e "quota" -e "READ_SCOPE_BROAD" -e "overlay" -e "Troubleshooting" docs/llm-wikis.md`; (2) map each plan Task 16 Step 1–3 bullet to one exact line; (3) record the mapping table. | Every required bullet maps to at least one accurate line; no mapping is missing and no line claims an unsupported platform, load mode, provider row, or security boundary. | PENDING |  |
| OFF-126 | Documentation identifies Codex installed plugins as deferred under `--ignore-user-config`, requires overlay redeployment after APM sync, and requires new live doctor after fingerprint change. | All | offline | Closed procedure: (1) run `rg -n -e "ignore-user-config" -e "installed plugin" -e "APM" -e "deploy" -e "fingerprint" -e "doctor --live" docs/llm-wikis.md`; (2) identify one line for each required statement; (3) compare supported alternatives with spec Sections 6.2 and 6.5. | Six searches return matching lines; docs explicitly defer Codex installed plugins, require overlay redeployment after every APM sync, state fingerprint invalidation, require a new live doctor, and list only supported project-skill/Claude-local-plugin alternatives. | PENDING |  |
| OFF-127 | `development-handoff.md` remains absent and no MCP or orchestration production code is included. | All | offline | Run `rg --files -g "development-handoff.md"` and inspect Cargo/source targets for MCP or orchestration modules. | No handoff file is found and no excluded implementation exists. | PENDING |  |
| OFF-128 | Disposable Phase 0 independence and test-first chronology are a mechanical roll-up over objective dependency-isolation, README ownership, source-lineage comparison, RED chronology, and provider-discovery evidence. | All | offline | Closed procedure: (1) parse this checklist; (2) require PASS with non-empty evidence for OFF-145, OFF-146, PROC-029, and PROC-030; (3) inspect OFF-145 evidence for the README declaration, dependency/import scan, exact-hash comparison, copy-similarity report, and per-file source-lineage manifest; (4) fail on any absent, PENDING, or FAIL prerequisite/artifact. | PASS only when all four prerequisite rows are PASS and OFF-145 contains every named objective artifact proving evidence-only/no-copy isolation; prose assertions or hashes without lineage/similarity evidence fail this row. | PENDING |  |
| OFF-129 | Preflight records sanitized commands, provider versions, output-shape labels, results, and unresolved rows using synthetic content only. | All | offline | Closed procedure: (1) parse `docs/verification/llm-wikis-preflight.md` and linked fixtures; (2) require command, exit, timestamp, platform, version, PASS/FAIL/PENDING, and evidence path for every Task 2 spike; (3) scan for LIVE-010 forbidden fields; (4) require provider content fixtures to use only approved synthetic roots/slugs. | Every Task 2 spike has the required metadata; unavailable rows are PENDING; evidence retains only native-format labels, validation booleans/counts, approved synthetic slugs, and digests; real slugs/page names, answers, prompts, payloads, account/session IDs, tokens, real prose, and absolute protected paths are absent. | PENDING |  |
| OFF-130 | Local provider flags/output and process primitive passed before affected production implementation; any failed assumption stopped the path for spec/plan correction and re-review. | Local implementation platform | offline | Closed procedure: (1) parse timestamps/status/evidence for PROC-028 and its nine prerequisites; (2) parse the first Task 3 production timestamp; (3) require every locally mandatory row PASS earlier than Task 3; (4) for each FAIL, require a later approved spec/plan replacement and repeated review before any affected production timestamp. | Every mandatory local row is PASS before Task 3; no FAIL precedes affected production without an approved corrected spec/plan and completed repeat review; missing chronology fails. | PENDING |  |
| OFF-131 | Version 0.1.0 does not externalize initialization, ingestion, update, merge, lint, audit, answer filing, or any wiki skill other than query. | All | offline | Closed procedure: (1) run `cargo run -- --help`; (2) run `rg -n -e "init wiki" -e "ingest" -e "update" -e "merge" -e "lint" -e "audit" -e "answer fil" src Cargo.toml config.example.toml docs/llm-wikis.md`; (3) inspect every match as CLI/API implementation or explicit deferral. | Help exposes none of the excluded operations; source/Cargo/config contain no callable implementation; every docs match is an explicit out-of-scope statement. | PENDING |  |
| OFF-132 | Version 0.1.0 does not automatically discover installed skills or plugins. | All | offline | Closed procedure: (1) run `rg -n -e "discover" -e "enumerate" -e "installed.*skill" -e "installed.*plugin" src config.example.toml docs/llm-wikis.md`; (2) inspect config resolver/provider filesystem scans; (3) trace entrypoint selection from parsed config to prompt. | Every selected entrypoint/artifact originates in explicit trusted configuration; no scan enumerates installed skills/plugins; docs matches only describe deferral. | PENDING |  |
| OFF-133 | Version 0.1.0 provides no HTTP service. | All | offline | Closed procedure: (1) run `cargo metadata --no-deps --format-version 1`; (2) run `rg -F -n -e "TcpListener" -e "HttpServer" -e "hyper" -e "axum" -e "warp" -e "route(" -e "listen(" Cargo.toml src`; (3) run `cargo run -- --help`; (4) run `rg -F -n -e "HTTP service" -e "out of scope" docs/llm-wikis.md`. | Metadata exits 0 with one CLI binary and no HTTP dependency/target; fixed-string source scan exits 1 with empty stdout; help exits 0 with no service command; docs scan contains an explicit HTTP-service deferral. | PENDING |  |
| OFF-134 | Version 0.1.0 provides no remote-execution facility. | All | offline | Closed procedure: (1) run `rg -n -e "ssh" -e "remote_host" -e "remote execution" -e "rpc" -e "tonic" Cargo.toml src config.example.toml docs/llm-wikis.md`; (2) run CLI help; (3) inspect every match. | Source/Cargo/config/help expose no remote host, SSH, RPC, or remote command path; docs matches only state deferral; provider processes execute locally. | PENDING |  |
| OFF-135 | Version 0.1.0 provides no multi-user tenancy. | All | offline | Closed procedure: (1) run `rg -n -e "tenant" -e "multi-user" -e "user_id" -e "authorization" -e "rbac" Cargo.toml src config.example.toml docs/llm-wikis.md`; (2) inspect public config/models and CLI help; (3) inspect every match. | No tenant/user identity, shared authorization, RBAC, or tenancy storage/API exists; docs matches only state local single-operator scope/deferral. | PENDING |  |
| OFF-136 | Version 0.1.0 does not change the existing wiki page format or formal citation format. | All | offline | Closed procedure: (1) hash upstream skill/schema/page-format sources before and after implementation using the frozen implementation manifest; (2) run `rg -n -e "## Citations" -e "raw/" -e "assets/" agents/overrides/wiki-query/SKILL.md src docs/llm-wikis.md`; (3) inspect every format-producing code path; (4) compare overlay/deployed copies under OFF-124. | Upstream format-source hashes are unchanged; no code rewrites page/formal-footnote syntax; wrapper normalization affects only public page-provenance objects; docs explicitly preserve existing formats. | PENDING |  |
| OFF-137 | Each query profile table has a closed schema: required `contract`, optional `claude` and `codex` subtables, and no load-mode/provider/executable fields at profile level. | All | offline | Run `cargo test --test config_contract off_137 -- --exact`. Fixture/assertion scope: Run strict-config cases for a valid profile, missing/mistyped contract, unknown fields, and cross-level load, entrypoint, skill_path, plugin_dir, or executable fields; inspect provider call count. | Valid profile parses; every missing required, mistyped, unknown, or cross-level field returns `CONFIG_INVALID` before provider startup. | PENDING |  |
| OFF-138 | A Claude project_skill table has exactly required load=`project_skill`, entrypoint, and skill_path fields; local-plugin-only fields are inapplicable. | All | offline | Run `cargo test --test config_contract off_138 -- --exact`. Fixture/assertion scope: Run strict-config cases omitting or mistyping each required field and adding plugin_dir or other local-plugin/cross-mode fields; inspect provider call count. | Only the closed Claude project_skill shape parses; every missing, mistyped, unknown, or cross-mode field returns `CONFIG_INVALID` before provider startup. | PENDING |  |
| OFF-139 | A Claude local_plugin table has exactly required load=`local_plugin`, entrypoint, plugin_dir, and skill_path fields; project-skill-only or unrelated fields are inapplicable. | All | offline | Run `cargo test --test config_contract off_139 -- --exact`. Fixture/assertion scope: Run strict-config cases omitting or mistyping each required field and adding project-root-relative or other cross-mode fields; inspect provider call count. | Only the closed Claude local_plugin shape parses; every missing, mistyped, unknown, or cross-mode field returns `CONFIG_INVALID` before provider startup. | PENDING |  |
| OFF-140 | A Codex project_skill table has exactly required load=`project_skill`, entrypoint, and skill_path fields; plugin_dir, local_plugin, and installed-plugin fields are inapplicable. | All | offline | Run `cargo test --test config_contract off_140 -- --exact`. Fixture/assertion scope: Run strict-config cases omitting or mistyping each required field and adding plugin_dir, local-plugin, installed-plugin, or other cross-mode fields; inspect provider call count. | Only the closed Codex project_skill shape parses; every missing, mistyped, unknown, or cross-mode field returns `CONFIG_INVALID` before provider startup. | PENDING |  |
| OFF-141 | Static doctor validates that Claude local-plugin manifest identity exists and exactly matches the plugin namespace in the configured entrypoint. | All | offline | Run `cargo test --test doctor off_141 -- --exact`. Fixture/assertion scope: Run static doctor with missing, malformed, mismatched-name, and exact-match `.claude-plugin/plugin.json` fixtures while counting provider starts. | Test exits 0 after asserting the first three fixtures return `ENTRYPOINT_INVALID` with zero paid invocations and exact-match fixture emits entrypoint check status pass before executable/auth checks. | PENDING |  |
| OFF-142 | Probe-store JSON uses a closed schema with schema_version 1, records array, every documented record field, and RFC 3339 UTC verified_at. | All | offline | Run `cargo test --test probes off_142 -- --exact`. Fixture/assertion scope: Serialize and parse golden probe stores; remove or mistype each top-level/record field, add unknown fields, and test non-UTC or invalid timestamps; then run normal query with a paid-call counter. | Canonical store contains exactly schema_version and records; each record contains exactly wiki_id, canonical_project_root, canonical_content_root, agent, agent_executable, agent_version, profile, contract, load, entrypoint, skill_fingerprint, compatibility_fingerprint, and verified_at; verified_at is RFC 3339 UTC; malformed/unknown/missing/mistyped stores are unverified and query returns `ENTRYPOINT_UNVERIFIED` before provider startup. | PENDING |  |
| OFF-143 | Unicode and space-containing paths work across absolute `--config`, project/content roots, skill_path or plugin_dir, and absolute provider executable resolution/invocation. | Windows, Linux/WSL, macOS | offline | Run `cargo test --test config_contract off_143 -- --exact`. Fixture/assertion scope: Run cross-platform configuration, canonical-resolution, static-doctor, and fixture-query tests with Traditional Chinese and spaces in every listed path category for project skill and Claude local plugin. | Paths remain exact through canonicalization and structured invocation; valid contained fixtures succeed without mojibake, cwd fallback, shell interpretation, or false containment failure. | PENDING |  |
| OFF-144 | A compiled query launched from an unrelated temporary cwd returns a valid normalized result using only the explicit absolute config, without cwd discovery or manual copy-paste. | Windows, Linux/WSL, macOS | offline | Run `cargo test --test cli_contract off_144 -- --exact`. Fixture/assertion scope: Build the binary; create an unrelated temporary cwd containing a decoy project/config; invoke the compiled binary there with an absolute fixture config and fixture provider; parse stdout as one JSON result. | Exit is 0; result satisfies the normalized query schema with validated citation or valid no_relevant_material gap; decoy cwd is untouched/ignored and the answer is returned directly by the command. | PENDING |  |
| OFF-145 | Spike code is objectively declared evidence-only and dependency/source-lineage isolated from production code. | All | offline | Closed procedure: (1) run `rg -F -n -e "evidence only" -e "cannot be copied into production" spikes/README.md`; (2) run `pwsh -NoProfile -Command '$m = cargo metadata --format-version 1 \| ConvertFrom-Json; $packages = @($m.packages \| Where-Object { $m.workspace_members -contains $_.id } \| Sort-Object name,version \| ForEach-Object { [ordered]@{ name = $_.name; version = $_.version; dependencies = @($_.dependencies \| Sort-Object name,kind \| ForEach-Object { [ordered]@{ name = $_.name; kind = if ($null -eq $_.kind) { "normal" } else { $_.kind }; optional = [bool]$_.optional } }) } }); [ordered]@{ schema_version = 1; packages = $packages } \| ConvertTo-Json -Depth 6 \| Set-Content -LiteralPath "docs/verification/evidence/llm-wikis-v0.1.0/OFF-145/off-145-dependencies.json" -Encoding utf8NoBOM'`; raw metadata remains in memory and is never persisted; (3) parse that projection and require exactly top-level `schema_version` and `packages`, exactly `name`, `version`, and `dependencies` per package, and exactly `name`, `kind`, and `optional` per dependency; reject every other key, any absolute/UNC/drive-rooted path value, and any dependency named `llm-wikis-spikes`; (4) run `rg -n -e "spikes/" -e "llm-wikis-spikes" Cargo.toml Cargo.lock src tests`; (5) write sorted SHA-256 manifests for production and spike Rust files and compare exact hash intersections; (6) run `git diff --no-index --find-copies-harder --find-copies=50% --summary spikes/src src` and record its output/exit; (7) in Git mode, for every production `src/**/*.rs` compute slash-normalized repository-relative `$relative`, run `git log --follow --reverse --format='%H%x09%cI' -- $relative`, require at least one record, select its first record as the explicitly oldest commit/timestamp, and join the same relative file to its earlier OFF-146 RED-evidence reference; (8) in the approved no-Git mode, select the first chronological checkpoint SHA-256/timestamp for every production Rust file and join the same relative file to its earlier OFF-146 RED-evidence reference; (9) write `off-145-lineage.json` with exactly top-level `schema_version`, `mode`, and `records` and exactly `file`, `oldest_revision_kind`, `oldest_revision`, `oldest_recorded_at`, and `red_evidence_ref` per record, where mode/kind are `git`/`git_commit` with a 40-hex revision or `no_git`/`checkpoint_sha256` with a 64-hex revision, times are RFC 3339, file and evidence references are slash-normalized repository-relative values, and there is exactly one record per production Rust file; (10) reject raw Git history, raw metadata, absolute paths, duplicate/missing file records, a non-oldest revision, or a RED timestamp not earlier than its joined oldest timestamp. | README scan returns both declarations; the dependency projection has only the allowed fields, contains no absolute path or spike dependency, and raw metadata is absent; import scan has no production dependency/import match; production/spike exact-hash intersection is empty; copy-similarity report contains no `copy from spikes/`; structured lineage has exactly one sanitized relative record per production Rust file with a reproducible oldest revision/timestamp and earlier RED-evidence reference in either approved mode; every command/output and join artifact is preserved. | PENDING |  |
| OFF-146 | Production implementation has timestamped RED-before-production evidence independent of spike code. | All | offline | Closed procedure: (1) for Tasks 3–13, read each execution checkpoint's failing-test command, exit status, timestamp, implementation-start timestamp, and implementation file hash/commit; (2) require failing-test timestamp earlier than the first production-file hash/commit for that task; (3) require the failing tests reside outside `spikes/`; (4) write the ordered comparison table to evidence. | Every production task has an exact non-spike test command with nonzero RED exit recorded before its first implementation change; missing, unordered, or prose-only chronology fails this row. | PENDING |  |

## Process checks

| ID | Requirement | Platform | Phase | Exact command or inspection | Expected result | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PROC-001 | Provider invocation uses a structured argument vector and sends the complete prompt only through stdin, never through a shell command string. | All | process | Run `cargo test --test process_supervisor proc_001 -- --exact`. Fixture/assertion scope: Run adversarial process tests and inspect ProcessRequest construction for both adapters. | Provider argv contains only fixed trusted arguments and resolved paths; question/prompt bytes appear only on stdin. | PENDING |  |
| PROC-002 | Each provider starts in its own Unix process group or Windows Job Object or equivalently tested containment. | Windows, Linux/WSL, macOS | process | Run `cargo test --test process_supervisor proc_002 -- --exact`. Fixture/assertion scope: Run platform process-helper tests that spawn a child and grandchild and inspect containment/cleanup. | Whole test tree is contained independently of the wrapper process. | PENDING |  |
| PROC-003 | Supervisor writes the complete stdin payload and closes stdin. | All | process | Run `cargo test --test process_supervisor proc_003 -- --exact`. Fixture/assertion scope: Run process-helper echo/EOF tests with Traditional Chinese, multiline data, and payload larger than one write buffer. | Helper receives byte-identical complete input and observes EOF. | PENDING |  |
| PROC-004 | Supervisor drains stdout and stderr concurrently without deadlock. | All | process | Run `cargo test --test process_supervisor proc_004 -- --exact`. Fixture/assertion scope: Run helper mode that fills both pipes beyond normal pipe capacity before exiting. | Command completes within deadline with both bounded streams captured and no deadlock. | PENDING |  |
| PROC-005 | stdout and stderr limits are enforced independently while streaming and identify the offending stream. | All | process | Run `cargo test --test process_supervisor proc_005 -- --exact`. Fixture/assertion scope: Run helper modes exceeding each cap by one byte while the other stream remains valid. | Process tree terminates; error is `OUTPUT_TOO_LARGE`; details name the correct stream and counts. | PENDING |  |
| PROC-006 | One monotonic deadline spans spawn, stdin write, reads, wait, termination, and reap. | All | process | Run `cargo test --test process_supervisor proc_006 -- --exact`. Fixture/assertion scope: Run helper delays at each lifecycle stage under a controlled monotonic clock. | Total operation respects one deadline and returns `TIMEOUT` without wall-clock regression errors. | PENDING |  |
| PROC-007 | Timeout, overflow, cancellation, and parser-aborting failure kill and reap the entire process tree. | Windows, Linux/WSL, macOS | process | Run `cargo test --test process_supervisor proc_007 -- --exact`. Fixture/assertion scope: Run process-helper grandchild sentinel tests for every failure path and probe all emitted PIDs afterward. | Wrapper waits for termination; no child/grandchild remains alive; failure is not reported as success. | PENDING |  |
| PROC-008 | Supervisor joins both pipe readers before returning and preserves bounded stdout, stderr, exit status, elapsed time, and termination reason as separate fields. | All | process | Run `cargo test --test process_supervisor proc_008 -- --exact`. Fixture/assertion scope: Run ProcessOutcome integration tests for success, nonzero, timeout, and overflow. | Every field is distinct and internally consistent; no stream is merged or read after return. | PENDING |  |
| PROC-009 | Direct executables start without a shell; only a trusted Windows `.cmd` or `.bat` shim uses the dedicated batch adapter with fixed argv and stdin question transport. | Windows x64 | process | Run `cargo test --test executable_resolution proc_009 -- --exact`. Fixture/assertion scope: Run resolution/invocation fixtures for `.exe`, `.cmd`, and `.bat` paths containing spaces and `%!^&` characters. | `.exe` is direct; batch shim preserves fixed command shape; question never appears in command-line text. | PENDING |  |
| PROC-010 | Generated empty MCP config, Codex schema, and batch helpers use a fresh canonical system temp directory outside all configured roots with exclusive creation, guarded ownership, and cleanup after child reap. | Windows, Linux/WSL, macOS | process | Run `cargo test --test process_supervisor proc_010 -- --exact`. Fixture/assertion scope: Run temp-artifact tests with unsafe/colliding temp roots and normal success/failure invocations. | Unsafe location fails before provider startup; safe artifacts are exclusive, permission-restricted where supported, held through startup, and removed after reap. | PENDING |  |
| PROC-011 | Claude JSON schema is passed as an implementation-generated inline string, not a path. | All | process | Run `cargo test --test claude_adapter proc_011 -- --exact`. Fixture/assertion scope: Inspect captured Claude argv from adapter golden tests. | `--json-schema` value is inline JSON and no schema temp path is supplied. | PENDING |  |
| PROC-012 | Authentication readiness uses `claude auth status --json` and `codex login status`, never a paid request or logout. | All | process | Run `cargo test --test claude_adapter --test codex_adapter proc_012 -- --exact`. Fixture/assertion scope: Inspect fake-runner call vectors and preflight command evidence. | Exact non-billable status commands run; no logout or model invocation is used for auth readiness. | PENDING |  |
| PROC-013 | Auth nonzero or explicit logged-out state maps to `AUTH_REQUIRED`; exit-zero malformed status maps to `INVALID_NATIVE_OUTPUT`; timeout/overflow retain their classes. | All | process | Run `cargo test --test claude_adapter --test codex_adapter proc_013 -- --exact`. Fixture/assertion scope: Run table-driven auth parser/process tests for both providers. | Both test targets exit 0 after asserting nonzero/logged-out is `AUTH_REQUIRED` exit 3, malformed exit-zero is `INVALID_NATIVE_OUTPUT` exit 6, timeout is exit 5, and either stream overflow is `OUTPUT_TOO_LARGE` exit 5. | PENDING |  |
| PROC-014 | Ordinary paid provider nonzero exit maps to `NONZERO_EXIT`, distinct from auth-status handling. | All | process | Run `cargo test --test claude_adapter --test codex_adapter proc_014 -- --exact`. Fixture/assertion scope: Invoke both adapters with valid auth fixtures and a paid child that exits nonzero. | Result is `NONZERO_EXIT`, exit 4, with bounded sanitized diagnostics. | PENDING |  |
| PROC-015 | Claude runs with cwd project_root and the exact minimum non-interactive read-only argument vector from the current approved capability fixture. | All | process | Run `cargo test --test claude_adapter proc_015 -- --exact`. Fixture/assertion scope: Compare captured Claude ProcessRequest against the approved preflight fixture and specification Section 10.2. | Cwd is canonical project_root; required add-dir, print, input, session, permission, tools, MCP, output, and schema options match the approved versioned vector. | PENDING |  |
| PROC-016 | Claude local_plugin adds only the configured `--plugin-dir` load argument while keeping the same safety vector. | All | process | Run `cargo test --test claude_adapter proc_016 -- --exact`. Fixture/assertion scope: Diff captured Claude project-skill and local-plugin argv fixtures. | Only the required plugin-dir pair differs for load mode; entrypoint remains on stdin. | PENDING |  |
| PROC-017 | Claude never uses dangerously-skip-permissions and never exposes Bash, Edit, Write, Web, MCP, subagents, or session persistence. | All | process | Run `cargo test --test claude_adapter proc_017 -- --exact`. Fixture/assertion scope: Inspect captured argv, generated MCP config, and tool allowlist across every Claude mode. | Only Read, Grep, and Glob are exposed; empty strict MCP and no-session behavior are enforced. | PENDING |  |
| PROC-018 | Claude parser accepts one native JSON document, rejects `is_error` or non-success subtype, prefers structured_output, validates the result, and caps stderr diagnostics. | All | process | Run `cargo test --test claude_adapter proc_018 -- --exact`. Fixture/assertion scope: Run Claude parser fixtures for valid structured output, fallback/error subtype, malformed JSON, nonzero, long stderr, timeout, and overflow. | Test exits 0 after asserting valid structured output yields claude-json; `is_error`, non-success subtype, and malformed JSON yield `INVALID_NATIVE_OUTPUT`; nonzero yields `NONZERO_EXIT`; timeout yields `TIMEOUT`; cap crossing yields `OUTPUT_TOO_LARGE`; diagnostics stay within cap. | PENDING |  |
| PROC-019 | Every Claude static doctor and query reports exact `CLAUDE_READ_SCOPE_BROAD` warning. | All | process | Run `cargo test --test claude_adapter proc_019 -- --exact`. Fixture/assertion scope: Execute fixture static doctor and query and compare warning golden text. | Code/message are exact and explain project-root read reach plus OS sandbox/container mitigation. | PENDING |  |
| PROC-020 | Codex uses the approved structured vector with top-level `--ask-for-approval never` before `exec`, project cwd, read-only sandbox, ephemeral mode, ignored user config, JSONL, schema file, stdin marker, and no `--add-dir`. | All | process | Run `cargo test --test codex_adapter proc_020 -- --exact`. Fixture/assertion scope: Compare captured Codex ProcessRequest with the approved preflight fixture and specification Section 10.3. | Argument order and values are exact; no writable add-dir or user-config hardening dependency appears. | PENDING |  |
| PROC-021 | Codex parser parses every non-empty stdout line as JSON, rejects error/failed-turn events, selects the last completed agent message, and validates it against the contract. | All | process | Run `cargo test --test codex_adapter proc_021 -- --exact`. Fixture/assertion scope: Run Codex JSONL fixtures for valid multiple messages, malformed line, error event, failed turn, no final message, and schema violation. | Test exits 0 after asserting valid stream selects last completed message and codex-jsonl; malformed/error/failed-turn yields `INVALID_NATIVE_OUTPUT`; missing completed message yields `NO_FINAL_MESSAGE`; final schema violation yields `CONTRACT_VIOLATION`. | PENDING |  |
| PROC-022 | Codex caps event diagnostics/stderr and treats read-only sandbox as write prevention rather than a strict read allowlist. | All | process | Run `cargo test --test codex_adapter proc_022 -- --exact`. Fixture/assertion scope: Run oversized diagnostic fixtures and inspect docs/warnings/adapter policy. | Output remains bounded and exact `CODEX_READ_SCOPE_BROAD` warning recommends an OS sandbox/container, not a user permission profile. | PENDING |  |
| PROC-023 | The exact three-line adversarial UTF-8 fixture is `繁體中文`, `--leading-dash`, and ``"quotes" `backticks` $() & \| < > ^ % !``; it cannot alter argv or launch extra processes. | Windows, Linux/WSL, macOS | process | Run `cargo test --test process_supervisor proc_023 -- --exact`. Fixture/assertion scope: Run provider-adapter integration tests with the exact plan Task 2 Step 4 byte fixture, a sentinel executable, and captured argv/stdin/process tree. | Stdin recovers every byte and line break exactly, including literal ASCII pipe 0x7C and exclamation 0x21; fixed argv is identical and contains no question byte substring; no sentinel side-effect process or file appears. | PENDING |  |
| PROC-024 | Executable resolution supports trusted command names and absolute paths, including spaces, Windows PATHEXT shims, and representative shell metacharacters without command-shape changes. | Windows, Linux/WSL, macOS | process | Run `cargo test --test executable_resolution proc_024 -- --exact`. Fixture/assertion scope: Run isolated PATH/PATHEXT resolution and invocation fixtures on each platform. | Canonical expected executable is selected and invoked safely; no path text is evaluated as shell syntax. | PENDING |  |
| PROC-025 | Provider safety-vector incompatibility cannot be fixed by a permission mode that re-enables writes, shell, web, MCP, subagents, sessions, or escalation. | All | process | Run `cargo test --test claude_adapter --test codex_adapter proc_025 -- --exact`. Fixture/assertion scope: Inspect approved Phase 0 capability fixture, provider help evidence, and any compatibility changes. | Minimum invocation capability and all safety invariants remain; incompatible provider versions fail closed pending spec/plan review. | PENDING |  |
| PROC-026 | The product remains synchronous Rust while bounded concurrent pipe readers and platform supervision provide lifecycle control without an async application runtime. | All | process | Run `cargo test --test process_supervisor proc_026 -- --exact`. Fixture/assertion scope: Inspect Cargo dependencies and process implementation; run all process tests. | No async application runtime is required and concurrency/lifecycle properties pass. | PENDING |  |
| PROC-027 | A failed process-tree termination or reap returns `TERMINATION_FAILED` rather than masking the failure or returning provider content. | Windows, Linux/WSL, macOS | process | Run `cargo test --test process_supervisor proc_027 -- --exact`. Fixture/assertion scope: Use an injectable termination primitive to force kill/reap confirmation failure. | Error is `TERMINATION_FAILED`, exit 5, and no provider answer is returned. | PENDING |  |
| PROC-028 | The mandatory local Phase 0 capability gate is a mechanical roll-up over the independently executable provider-discovery, capability-surface, stdin, resolution, directory, pipe, deadline/tree, and mutation spike rows. | Local implementation platform | process | Closed procedure: (1) parse this checklist; (2) require PASS with non-empty evidence for PROC-029, PROC-030, PROC-031, PROC-032, PROC-033, PROC-034, PROC-035, PROC-036, and PROC-037; (3) inspect PROC-036 evidence for configured deadline/sleep, exact `TIMEOUT` and termination reason, bounded elapsed time, and dead-PID probes; (4) fail on any absent, PENDING, or FAIL prerequisite/artifact; (5) compare latest prerequisite timestamp with first Task 3 production timestamp. | PASS only when all nine prerequisites passed before Task 3 and PROC-036 proves the exact 500 ms deadline, 5000 ms child delay, `TIMEOUT`, `deadline_exceeded`, 500–2000 ms elapsed bound, and zero live child/grandchild PIDs; any omission keeps the gate PENDING or FAIL. | PENDING |  |
| PROC-029 | Before production, a disposable Claude fixture proves project-skill discovery using distinct temporary project_root and contained content_root without importing or modifying the future production overlay. | Local implementation platform with Claude | process | Run `cargo run --manifest-path spikes/Cargo.toml -- provider-fixture-query`. Inspect the Claude-labeled result, timestamp, fixture roots, overlay-dependency scan, resolvable synthetic citation, and before/after digest fields. | Command exits 0; Claude record predates production; roots are distinct/contained; native-format label is claude-json; contract validates with approved synthetic citation; digests match; overlay dependency count is zero. | PENDING |  |
| PROC-030 | Before production, a disposable Codex fixture proves project-skill discovery using distinct temporary project_root and contained content_root without importing or modifying the future production overlay. | Local implementation platform with Codex | process | Run `cargo run --manifest-path spikes/Cargo.toml -- provider-fixture-query`. Inspect the Codex-labeled result, timestamp, fixture roots, overlay-dependency scan, resolvable synthetic citation, and before/after digest fields. | Command exits 0; Codex record predates production; roots are distinct/contained; native-format label is codex-jsonl; contract validates with approved synthetic citation; digests match; overlay dependency count is zero. | PENDING |  |
| PROC-031 | Before production, installed Claude and Codex executable/version/help/auth surfaces and minimum safe capability vectors are recorded and pass locally. | Local implementation platform | process | Closed procedure: (1) run `claude --version`, `claude --help`, `claude auth status --json`, `codex --version`, `codex --help`, `codex exec --help`, and `codex login status`; (2) record exact command, exit, canonical executable, version, accepted required flags, redacted auth classification, and excluded capabilities; (3) compare against spec Sections 10.2–10.3. | Every command has timestamped sanitized output; both required vectors are accepted; auth is classified deterministically; Write, Edit, shell, web, MCP, subagents, sessions, and escalation remain excluded; any mismatch is FAIL. | PENDING |  |
| PROC-032 | Before production, the exact adversarial stdin fixture round-trips without question bytes in argv. | Local implementation platform | process | Run `cargo run --manifest-path spikes/Cargo.toml -- stdin-boundary` with the exact three lines from PROC-023 and save captured stdin/argv hashes. | Exit is 0; recovered stdin hash equals input hash byte-for-byte; captured argv contains no complete question line or question byte sequence. | PENDING |  |
| PROC-033 | Before production on Windows, native and batch-shim executable resolution preserves exact fixed argv through paths containing spaces and metacharacters. | Windows local implementation platform | process | Run `cargo run --manifest-path spikes/Cargo.toml -- windows-resolution`; record native `.exe`, `.cmd` or `.bat`, spaced-path, and `%!^&` fixture cases separately. | Exit is 0; every fixture reports the expected Native or BatchShim class and exact fixed argv; no shell expansion occurs. | PENDING |  |
| PROC-034 | Before production, all six platform config/cache directory resolutions are exact and cwd-independent. | Local implementation platform | process | Run `cargo run --manifest-path spikes/Cargo.toml -- platform-dirs` from two unrelated cwd values using fixed injected APPDATA, XDG_CONFIG_HOME, HOME, and macOS-home fixtures. | Exit is 0; both cwd runs return identical exact Windows, Linux/XDG-fallback, and macOS config/cache paths from spec Sections 5.2 and 15.1. | PENDING |  |
| PROC-035 | Before production, concurrent bounded output proves no deadlock and independent stdout/stderr overflow classification. | Local implementation platform | process | Run `cargo run --manifest-path spikes/Cargo.toml -- bounded-pipes` for dual-pipe pressure, stdout-limit-plus-one, and stderr-limit-plus-one fixture modes. | Exit is 0 for the spike harness; dual pressure completes without deadlock; overflow cases report exact offending stream, limit, observed bytes, and terminated tree. | PENDING |  |
| PROC-036 | Before production, success, known-deadline timeout, and forced-overflow process trees are exactly classified, bounded, terminated, and reaped. | Local implementation platform | process | Run `cargo run --manifest-path spikes/Cargo.toml -- process-tree --timeout-ms 500 --child-sleep-ms 5000 --max-elapsed-ms 2000`. Read the emitted mode/result/elapsed/child-PID/grandchild-PID records and probe each PID after every mode. | Harness exits 0; success records `termination_reason=exited`; timeout records `code=TIMEOUT`, `termination_reason=deadline_exceeded`, and elapsed_ms from 500 through 2000; forced overflow records `code=OUTPUT_TOO_LARGE` with its offending stream; no recorded child or grandchild PID remains alive after any mode. | PENDING |  |
| PROC-037 | Before production, full-content hashing detects a same-size rewrite with restored timestamp. | Local implementation platform | process | Run `cargo run --manifest-path spikes/Cargo.toml -- mutation-hash`; save before/after length, timestamp, SHA-256, and changed relative path. | Exit is 0; length and timestamp are equal, SHA-256 differs, and the exact rewritten relative path is reported. | PENDING |  |

## Native-binary checks

| ID | Requirement | Platform | Phase | Exact command or inspection | Expected result | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| NAT-001 | The Windows release executable is native x86_64 MSVC, named `llm-wikis-windows-amd64.exe`, and passes `--version` plus offline CLI/process tests on Windows x64. | Windows x64 | native-binary | Closed procedure on `windows-2025`: (1) run `cargo build --release --target x86_64-pc-windows-msvc`; (2) copy the output to `llm-wikis-windows-amd64.exe`; (3) run `.\llm-wikis-windows-amd64.exe --version`; (4) configure the native process helper and run `cargo test --test cli_contract --test process_supervisor`. | All commands exit 0; PE architecture is x86-64; filename is exact; version stdout is exactly `llm-wikis 0.1.0`; CLI/process suites report zero failures. | PENDING |  |
| NAT-002 | The Linux/WSL release executable is static-musl x86_64, named `llm-wikis-linux-amd64`, and passes `--version` plus offline CLI/process tests on Ubuntu x64. | Linux/WSL x64 | native-binary | Closed procedure on `ubuntu-24.04`: (1) run `cargo build --release --target x86_64-unknown-linux-musl`; (2) copy output to `llm-wikis-linux-amd64`; (3) run `file llm-wikis-linux-amd64` and `ldd llm-wikis-linux-amd64`; (4) run `./llm-wikis-linux-amd64 --version`; (5) configure helper and run `cargo test --test cli_contract --test process_supervisor`. | All build/version/tests exit 0; file reports x86-64; linkage is static musl and ldd reports not dynamic; exact version line appears; suites report zero failures. | PENDING |  |
| NAT-003 | The macOS release executable is native Apple Silicon, named `llm-wikis-darwin-arm64`, and passes `--version` plus offline CLI/process tests on macOS ARM64. | macOS Apple Silicon | native-binary | Closed procedure on ARM64 `macos-15`: (1) run `cargo build --release --target aarch64-apple-darwin`; (2) copy output to `llm-wikis-darwin-arm64`; (3) run `file llm-wikis-darwin-arm64`; (4) run `./llm-wikis-darwin-arm64 --version`; (5) configure helper and run `cargo test --test cli_contract --test process_supervisor`. | All commands exit 0; Mach-O architecture is arm64; filename/version are exact; suites report zero failures. | PENDING |  |
| NAT-004 | Native process-tree termination and reap pass on Windows, Linux, and macOS using the separately built helper rather than a skipped/mock-only test. | Windows x64, Linux/WSL x64, macOS Apple Silicon | native-binary | Closed procedure on each native runner: (1) run `cargo build --manifest-path tests/fixtures/process-helper/Cargo.toml`; (2) set `LLM_WIKIS_PROCESS_HELPER` to the resolved native helper; (3) run `cargo test --test process_supervisor nat_004 -- --exact`; (4) probe every child/grandchild PID emitted by the test. | Each platform command exits 0; helper variable is non-empty; test is not skipped; no emitted PID remains alive. | PENDING |  |
| NAT-005 | Native default config/cache directories and executable resolution work on all supported platforms, including Windows `.exe` and `.cmd` and macOS zsh environment. | Windows x64, Linux/WSL x64, macOS Apple Silicon | native-binary | Run `cargo test --test config_contract nat_005 -- --exact` and `cargo test --test executable_resolution nat_005 -- --exact` on each native matrix runner. | Both commands exit 0 on all three runners and report exact platform path and executable-class assertions with zero skipped cases. | PENDING |  |
| NAT-006 | Intel macOS and Linux ARM are not published or claimed as supported in version 0.1.0. | Release metadata and docs | native-binary | Closed procedure: (1) run `rg -n -e "target:" -e "runs-on:" .github/workflows`; (2) list staged/Release asset names; (3) run installer unsupported-architecture cases; (4) run `rg -n -e "Intel" -e "Linux ARM" -e "Apple Silicon" docs/llm-wikis.md`. | Workflow/assets contain only Windows x64, Linux x64, and Darwin arm64; Intel macOS/Linux ARM installer cases fail nonzero; docs claim only Apple Silicon macOS. | PENDING |  |

## Installer checks

| ID | Requirement | Platform | Phase | Exact command or inspection | Expected result | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| INST-001 | PowerShell installer downloads latest by default and honors an explicit `LLM_WIKIS_VERSION`. | Windows x64 | installer | Run `pwsh -NoProfile -File tests/installers/verify-install-ps1.ps1` latest and pinned fake-release cases. | Correct latest or pinned Windows asset URL/version is selected. | PENDING |  |
| INST-002 | PowerShell installer supports Windows x64 only, verifies raw `.exe` against SHA256SUMS, runs `--version`, and installs to `%LOCALAPPDATA%\llm-wikis\bin\llm-wikis.exe`. | Windows x64 | installer | Run `pwsh -NoProfile -File tests/installers/verify-install-ps1.ps1 -Case INST-002`. | Command exits 0 after asserting success installs the exact hashed/smoke-tested binary and wrong architecture, checksum mismatch, and smoke failure each exit nonzero with prior destination unchanged. | PENDING |  |
| INST-003 | PowerShell installer updates user PATH only when the exact install directory is absent and never duplicates it. | Windows x64 | installer | Run `pwsh -NoProfile -File tests/installers/verify-install-ps1.ps1 -Case INST-003`. | Command exits 0 and asserts two installs leave exactly one normalized `%LOCALAPPDATA%\llm-wikis\bin` user-PATH entry with unrelated entries byte-preserved. | PENDING |  |
| INST-004 | PowerShell installer uses strict errors, unique temporary storage, cleans it on success/failure, and safely upgrades, downgrades, or repairs an existing binary. | Windows x64 | installer | Run `pwsh -NoProfile -File tests/installers/verify-install-ps1.ps1 -Case INST-004`. | Command exits 0 after success/failure cleanup, upgrade, downgrade, damaged-binary repair, and pre-smoke failure cases; no temp directory remains and destination changes only after checksum plus version smoke succeed. | PENDING |  |
| INST-005 | POSIX installer downloads latest by default and honors explicit `LLM_WIKIS_VERSION`. | Linux/WSL x64, macOS Apple Silicon | installer | Run `sh tests/installers/verify-install-sh.sh` latest and pinned fake-release cases on Ubuntu and macOS. | Correct platform asset and version endpoint are selected. | PENDING |  |
| INST-006 | POSIX installer accepts only Linux x86-64 and macOS ARM64 and gives explicit errors for Intel macOS and unsupported Linux architectures. | Linux/WSL, macOS | installer | Run `sh tests/installers/verify-install-sh.sh INST-006`. | Command exits 0 after asserting Linux x86-64 maps to `llm-wikis-linux-amd64`, Darwin arm64 maps to `llm-wikis-darwin-arm64`, and Darwin x86-64 plus Linux arm64 each fail before network/install. | PENDING |  |
| INST-007 | POSIX installer downloads raw binary plus SHA256SUMS, verifies with sha256sum or shasum, and fails closed if neither tool exists or checksum mismatches. | Linux/WSL x64, macOS Apple Silicon | installer | Run `sh tests/installers/verify-install-sh.sh INST-007`. | Command exits 0 after asserting sha256sum and shasum success cases and no-tool, missing-entry, duplicate-entry, malformed-manifest, and mismatch cases each fail before destination replacement. | PENDING |  |
| INST-008 | POSIX installer runs downloaded `--version` before installing to `~/.local/bin/llm-wikis`. | Linux/WSL x64, macOS Apple Silicon | installer | Run `sh tests/installers/verify-install-sh.sh INST-008`. | Command exits 0; success installs exact verified binary at isolated `~/.local/bin/llm-wikis`; smoke nonzero/wrong-version cases leave prior destination bytes unchanged. | PENDING |  |
| INST-009 | On macOS zsh, installer idempotently adds `~/.local/bin` to `~/.zprofile` only when needed. | macOS Apple Silicon | installer | Run `sh tests/installers/verify-install-sh.sh INST-009`. | Command exits 0 after two zsh installs; `.zprofile` has exactly one managed PATH line and unrelated bytes are preserved. | PENDING |  |
| INST-010 | On Linux bash or sh, installer idempotently adds `~/.local/bin` to `~/.profile` only when needed. | Linux/WSL x64 | installer | Run `sh tests/installers/verify-install-sh.sh INST-010`. | Command exits 0 for bash and sh cases after two installs each; `.profile` has exactly one managed PATH line and unrelated bytes are preserved. | PENDING |  |
| INST-011 | For unsupported shells the POSIX installer prints manual PATH instructions and does not modify unknown profiles. | Linux/WSL x64, macOS Apple Silicon | installer | Run `sh tests/installers/verify-install-sh.sh INST-011`. | Command exits 0 after asserting installation succeeds, stdout contains the exact manual PATH instruction, and every preexisting profile hash remains unchanged. | PENDING |  |
| INST-012 | POSIX installer uses `set -eu`, secure unique temp directory, quoted cleanup trap, `curl -fsSL`, cleanup on all paths, and safe repeated upgrade/downgrade/repair. | Linux/WSL x64, macOS Apple Silicon | installer | Run `sh tests/installers/verify-install-sh.sh INST-012`. | Command exits 0 after static primitive checks and success/download/checksum/smoke/install failure cleanup plus upgrade/downgrade/repair cases; destination changes only after checksum and smoke. | PENDING |  |
| INST-013 | Offline installer tests use a local fake Release origin only under explicit `LLM_WIKIS_INSTALLER_TEST=1`; production ignores or rejects alternate origins and uses the fixed GitHub repository. | Windows, Linux/WSL, macOS | installer | Closed procedure: (1) run `pwsh -NoProfile -File tests/installers/verify-install-ps1.ps1 -Case INST-013`; (2) run `sh tests/installers/verify-install-sh.sh INST-013`; (3) inspect captured requests with public network blocked. | Both commands exit 0; guarded alternate origin reaches only local fake server; unguarded override fails or uses fixed GitHub origin; captured offline-suite requests contain no public destination. | PENDING |  |
| INST-014 | Both installer suites pass twice without PATH/profile duplication. | Windows x64, Linux/WSL x64, macOS Apple Silicon | installer | Closed procedure: (1) in one isolated environment run `pwsh -NoProfile -File tests/installers/verify-install-ps1.ps1` twice on Windows; (2) run `sh tests/installers/verify-install-sh.sh` twice on Ubuntu and twice on macOS; (3) hash destinations and profiles after each run; (4) compare managed-entry counts and unrelated bytes. | All six platform-local runs exit 0; second-run binary is valid; managed PATH/profile entry count is exactly one; unrelated profile/PATH content is unchanged. | PENDING |  |

## Live-provider checks

| ID | Requirement | Platform | Phase | Exact command or inspection | Expected result | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| LIVE-001 | Current Claude project skill works through live doctor then normal query on Windows with explicit synthetic content_root, valid grounded contract/citations, and no mutation. | Windows x64 | live-provider | Run `cargo test --test doctor live_001 -- --ignored --exact`. Fixture/assertion scope: native Windows test creates an isolated temp config from `tests/fixtures/claude/synthetic`, invokes live doctor/query, compares digests, and writes only LIVE-010-approved evidence. | Both exits are 0; native format label is claude-json; `knowledge_status` is `grounded`; citation count is at least 1 and any retained slug is only `synthetic-page`; zero-citation or no_relevant_material output fails; digests match; forbidden fields are absent. | PENDING |  |
| LIVE-002 | Current Codex project skill works through live doctor then normal query on Windows with synthetic content, grounded JSONL normalization/citations, and no mutation. | Windows x64 | live-provider | Run `cargo test --test doctor live_002 -- --ignored --exact`. Fixture/assertion scope: native Windows test creates an isolated temp config from `tests/fixtures/codex/synthetic`, invokes live doctor/query, compares digests, and writes only LIVE-010-approved evidence. | Both exits are 0; native format label is codex-jsonl; `knowledge_status` is `grounded`; citation count is at least 1 with count or `synthetic-page` retained; zero-citation or no_relevant_material output fails; `CODEX_READ_SCOPE_BROAD` is recorded; digests match; forbidden fields are absent. | PENDING |  |
| LIVE-003 | Current Claude project skill works through live doctor then normal query on Linux/WSL using an approved synthetic content root and portable paths. | Linux/WSL x64 | live-provider | Run `cargo test --test doctor live_003 -- --ignored --exact`. Fixture/assertion scope: native Linux/WSL test uses `tests/fixtures/claude/synthetic`, isolated absolute temp paths, digest comparison, and LIVE-010 sanitizer. | Both exits are 0; native format label is claude-json; `knowledge_status` is `grounded`; citation count is at least 1 with count or `synthetic-page` retained; zero-citation or no_relevant_material output fails; paths resolve portably; digests match; forbidden fields are absent. | PENDING |  |
| LIVE-004 | Current Codex project skill works through live doctor then normal query on Linux/WSL using an approved synthetic content root with sandbox behavior recorded. | Linux/WSL x64 | live-provider | Run `cargo test --test doctor live_004 -- --ignored --exact`. Fixture/assertion scope: native Linux/WSL test uses `tests/fixtures/codex/synthetic`, isolated absolute temp paths, digest comparison, and LIVE-010 sanitizer. | Both exits are 0; native format label is codex-jsonl; `knowledge_status` is `grounded`; citation count is at least 1 with count or `synthetic-page` retained; zero-citation or no_relevant_material output fails; read-scope warning passes; digests match; forbidden fields are absent. | PENDING |  |
| LIVE-005 | Current Claude project skill works through the native ARM binary on macOS Apple Silicon with zsh and an approved synthetic content root. | macOS Apple Silicon | live-provider | Run `cargo test --test doctor live_005 -- --ignored --exact`. Fixture/assertion scope: native ARM macOS test launches `llm-wikis-darwin-arm64` from zsh against `tests/fixtures/claude/synthetic`, compares digests, and sanitizes evidence. | Both exits are 0; binary is arm64; native format label is claude-json; `knowledge_status` is `grounded`; citation count is at least 1 with count or `synthetic-page` retained; zero-citation or no_relevant_material output fails; digests match; forbidden fields are absent. | PENDING |  |
| LIVE-006 | Current Codex project skill works through the native ARM binary on macOS Apple Silicon with an approved synthetic content root and sandbox behavior recorded. | macOS Apple Silicon | live-provider | Run `cargo test --test doctor live_006 -- --ignored --exact`. Fixture/assertion scope: native ARM macOS test launches `llm-wikis-darwin-arm64` from zsh against `tests/fixtures/codex/synthetic`, compares digests, and sanitizes evidence. | Both exits are 0; binary is arm64; native format label is codex-jsonl; `knowledge_status` is `grounded`; citation count is at least 1 with count or `synthetic-page` retained; zero-citation or no_relevant_material output fails; read-scope warning passes; digests match; forbidden fields are absent. | PENDING |  |
| LIVE-007 | A differently named Claude local-plugin skill resolves its configured namespace against an approved synthetic content root and passes grounded live verification without forbidden plugin components. | Windows x64 or Linux/WSL x64 | live-provider | Run `cargo test --test doctor live_007 -- --ignored --exact`. Fixture/assertion scope: test builds a temp namespaced plugin from `tests/fixtures/claude/synthetic`, runs static/live/query stages, compares digests, and sanitizes evidence. | All exits are 0; configured namespace is exact; manifest checks pass; `knowledge_status` is `grounded`; citation count is at least 1 with count or `synthetic-page` retained; zero-citation or no_relevant_material output fails; digests match; forbidden fields are absent. | PENDING |  |
| LIVE-008 | A differently named Claude project skill works by configuration only against an approved synthetic content root with grounded citations. | Windows x64 or Linux/WSL x64 | live-provider | Run `cargo test --test doctor live_008 -- --ignored --exact`. Fixture/assertion scope: test builds `/synthetic-query` from `tests/fixtures/claude/synthetic`, runs live doctor/query, compares digests, and sanitizes evidence. | Both exits are 0; `/synthetic-query` is exact; `knowledge_status` is `grounded`; citation count is at least 1 with count or `synthetic-page` retained; zero-citation or no_relevant_material output fails; digests match; forbidden fields are absent. | PENDING |  |
| LIVE-009 | A differently named Codex project skill works by configuration only against synthetic content with grounded citations while a namespaced installed-plugin entrypoint fails closed. | Windows x64 or Linux/WSL x64 | live-provider | Run `cargo test --test doctor live_009 -- --ignored --exact`. Fixture/assertion scope: test builds `$synthetic-query` from `tests/fixtures/codex/synthetic`, runs live doctor/query plus installed-plugin rejection, compares digests, and sanitizes evidence. | Project-skill exits are 0; `knowledge_status` is `grounded`; citation count is at least 1 with count or `synthetic-page` retained; zero-citation or no_relevant_material output fails; digests match; installed-plugin config exits 2 with `ENTRYPOINT_INVALID` and zero provider starts; forbidden fields are absent. | PENDING |  |
| LIVE-010 | Every live-provider evidence artifact uses an approved synthetic content root and a strict sanitized evidence schema. | Each executed live platform/provider | live-provider | Run `cargo test --test doctor live_010 -- --exact`. Fixture/assertion scope: sanitizer parses every LIVE-001 through LIVE-009 and LIVE-011 artifact and enforces the closed allowed-key/content schema. | Every artifact uses synthetic content; only citation count/validation status or approved synthetic fixture slugs are retained; real citation slugs/page names, answers, prompts, native payloads, account/session IDs, tokens, real wiki prose, and absolute protected paths are absent. | PENDING |  |
| LIVE-011 | Live doctor is never implicit, failed live verification preserves the probe, and successful grounded verification enables only the exact tuple using synthetic content. | Each executed live platform/provider | live-provider | Run `cargo test --test doctor live_011 -- --ignored --exact`. Fixture/assertion scope: test uses the provider-specific synthetic fixture, hashes probe bytes around failed/successful doctor, changes one tuple field, runs query, and sanitizes evidence. | Failed doctor leaves hash unchanged; successful doctor/query has `knowledge_status=grounded` and citation count at least 1 or approved `synthetic-page`; zero-citation/no_relevant_material fails and publishes no probe; success enables exact tuple; changed tuple exits 3 with `ENTRYPOINT_UNVERIFIED`; normal query makes no probe call; forbidden evidence is absent. | PENDING |  |
| LIVE-012 | Documentation claims live support only for passed rows and exposes no unsanitized live evidence. | All support claims | live-provider | Run `cargo test --test doctor live_012 -- --exact`. Fixture/assertion scope: test parses LIVE statuses, docs/Release support claims, and linked evidence through LIVE-010 sanitizer. | Every live claim maps to a PASS row; PENDING/FAIL rows are unclaimed; binary support is separate; published/linked evidence contains only LIVE-010-approved synthetic metadata. | PENDING |  |

## Release checks

| ID | Requirement | Platform | Phase | Exact command or inspection | Expected result | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| REL-001 | Release automation runs only in a user-approved Git repository with GitHub remote, Actions, and Release/attestation permissions. | GitHub | release | Closed procedure: (1) locate explicit user approval timestamp/scope in execution.md; (2) run `git remote -v`; (3) run `pwsh -NoProfile -Command "$r=gh repo view --json nameWithOwner --jq .nameWithOwner; gh api ('repos/'+$r+'/actions/permissions')"`; (4) inspect release workflow permissions for contents/id-token; (5) compare all timestamps with first workflow publication. | Approval predates workflow publication; one intended GitHub remote exists; Actions is enabled; workflow has effective Release and attestation permissions; otherwise row remains PENDING. | PENDING |  |
| REL-002 | A `v0.1.0` tag triggers release and tag version must equal Cargo package version before build. | GitHub Actions | release | Closed procedure: (1) inspect `.github/workflows/release.yml` tag trigger; (2) run `cargo metadata --no-deps --format-version 1`; (3) execute workflow tag-version check with `v0.1.0` and `v0.1.1` fixture inputs. | Trigger matches `v*`; `v0.1.0` comparison exits 0; mismatch fixture exits nonzero before any build job starts. | PENDING |  |
| REL-003 | Tag path has an explicit prerequisite quality-gate job running format, Clippy with warnings denied, and the complete Rust tests with configured process helper. | GitHub Actions | release | Closed procedure: (1) inspect `release.yml` job needs graph; (2) inspect one tag run logs for `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, helper build/export, and `cargo test --all-targets --all-features`; (3) trace every build/attest/publish `needs`. | All four gate stages exit 0; helper is configured; every native build, attestation, and publish job directly or transitively requires the tag quality gate. | PENDING |  |
| REL-004 | CI uses native Windows 2025, Ubuntu 24.04, and ARM64 macOS 15 jobs with reviewed pinned major action versions and runs offline, process, and installer tests. | GitHub Actions | release | Closed procedure: (1) run `rg -n -e "windows-2025" -e "ubuntu-24.04" -e "macos-15" -e "uses:" .github/workflows/ci.yml .github/workflows/release.yml`; (2) inspect action major pins; (3) inspect successful native job logs for Rust, process-helper, and installer commands. | Exactly the three required native runner classes execute; action references use reviewed pinned majors; each runner logs offline/process/installer commands with exit 0 and no skipped platform substitute. | PENDING |  |
| REL-005 | Release builds exactly the three documented targets and raw executable names, with musl-tools for Linux. | GitHub Actions | release | Closed procedure: (1) run `rg -n -e "x86_64-pc-windows-msvc" -e "x86_64-unknown-linux-musl" -e "aarch64-apple-darwin" -e "musl-tools" -e "llm-wikis-windows-amd64.exe" -e "llm-wikis-linux-amd64" -e "llm-wikis-darwin-arm64" .github/workflows/release.yml`; (2) list staged artifacts. | Each target/name appears exactly in its native job; Ubuntu installs musl-tools; staged artifacts contain exactly three raw executables with no archive or unsupported binary. | PENDING |  |
| REL-006 | Each renamed native asset is smoke-tested on its native runner with exact `llm-wikis 0.1.0` output before upload. | Windows x64, Ubuntu x64, macOS Apple Silicon | release | Closed procedure: (1) inspect each native job's step order and logs; (2) require rename before native `--version`; (3) require exact stdout comparison before upload; (4) inspect a forced wrong-version fixture run and its skipped upload. | All three smoke commands exit 0 with exact line before upload; forced mismatch fails and upload step is skipped. | PENDING |  |
| REL-007 | SHA256SUMS contains exactly the three binaries, with no missing, duplicate, or unexpected entry; installer scripts remain raw repository files rather than Release assets. | GitHub Actions | release | Run `pwsh -NoProfile -File tests/release/verify-assets.ps1` on Windows and `sh tests/release/verify-assets.sh` on Linux/macOS against staged release files. | Both verifiers exit 0 only for exactly three correct checksum entries. | PENDING |  |
| REL-008 | Official GitHub artifact attestations cover all executables and the checksum manifest with `id-token: write` and `contents: read`; `gh` is not an installer prerequisite. | GitHub Actions | release | Closed procedure: (1) inspect workflow permission block and attestation subjects; (2) run GitHub attestation verification for each of three executable digests and SHA256SUMS digest; (3) run `rg -n "gh " install.ps1 install.sh`. | Permissions are exactly contents read and id-token write for attestation job; four subjects verify against published digests; installers contain no gh invocation/prerequisite. | PENDING |  |
| REL-009 | Publication occurs only after every Windows, Linux, and macOS native build, process, checksum, smoke, and installer row succeeds; no partial v0.1.0 asset set is published. | GitHub Release | release | Closed procedure: (1) parse workflow needs graph; (2) require PASS evidence for NAT-001 through NAT-006, INST-001 through INST-014, REL-003, REL-006, and REL-007; (3) inspect tag-run conclusions; (4) list Release assets. | Publish job is unreachable until every prerequisite succeeds; successful release contains exactly three binaries and SHA256SUMS; any absent/failed prerequisite keeps release unpublished. | PENDING |  |
| REL-010 | Versioned Release publication is no-clobber and its four asset names/digests are bound to a durable pre-publication manifest and attestations, with returned asset IDs recorded separately. | GitHub Release | release | Closed procedure: (1) inspect the tag-triggered `release.yml` and require the triggering tag to exist without causing failure; (2) require early failure only when a GitHub Release for the tag or any target asset name already exists; (3) require no edit, delete, overwrite, clobber, or replace API/path; (4) compare published names/digests with the frozen pre-publication manifest and attestations; (5) record API-returned asset IDs in a separate post-publication record and join them to names/digests. | Triggering tag proceeds; pre-existing Release or target asset makes workflow fail before upload; workflow has no existing-release mutation path; pre-publication manifest contains exactly four names/digests and no IDs; published names/digests equal manifest/attestations; four returned asset IDs are recorded only after publication and map one-to-one to those names/digests. | PENDING |  |
| REL-011 | CI and release workflows never invoke Claude or Codex and paid live-provider rows do not gate native binary publication. | GitHub Actions | release | Run `rg -n -e "claude" -e "codex" .github/workflows`; inspect matches and job dependencies. | Names appear only in comments asserting no invocation or not at all; no provider credential/call or LIVE job gates publish. | PENDING |  |
| REL-012 | macOS is documented as Apple Silicon-only unsigned preview and Windows as unsigned with possible SmartScreen warning; guidance does not disable platform security. | Documentation and Release notes | release | Closed procedure: (1) run `rg -n -e "Apple Silicon" -e "unsigned" -e "Gatekeeper" -e "SmartScreen" -e "checksum" -e "attestation" docs/llm-wikis.md`; (2) inspect installer messages and Release notes; (3) scan for Gatekeeper/SmartScreen disable commands. | Docs/notes state Apple Silicon-only unsigned preview and possible SmartScreen warning; normal user approval is described; no security-disable command exists; checksum/attestation are explicitly not code signing. | PENDING |  |
| REL-013 | No Apple notarization, Developer ID, Windows Authenticode, Intel macOS, Linux ARM, package-manager manifest, built-in updater, or uninstaller is claimed in v0.1.0. | Release artifacts and docs | release | Closed procedure: (1) run `rg -n -e "notar" -e "Developer ID" -e "Authenticode" -e "Intel" -e "Linux ARM" -e "Homebrew" -e "WinGet" -e "Scoop" -e "self-update" -e "uninstall" Cargo.toml src .github install.ps1 install.sh docs/llm-wikis.md`; (2) inspect every match and staged asset. | No implementation/manifest/signed/unsupported asset exists; every documentation match explicitly states exclusion or deferral. | PENDING |  |
| REL-014 | Final independent verification freezes implementation identity and records one sanitized artifact per executed row; failed or unavailable rows are never converted to pass through prose. | All | release | Closed procedure: (1) parse frozen commit or implementation SHA manifest; (2) parse all checklist statuses/evidence links; (3) require one existing artifact per PASS with command, exit, platform, timestamp, and sanitized output; (4) apply LIVE-010 to live artifacts; (5) compare verifier identity with OFF-001. | Freeze identity exists; every PASS has one complete artifact; live evidence is synthetic/sanitized; FAIL/PENDING are unchanged by prose; only the distinct Task 17 verifier edited status/evidence. | PENDING |  |
| REL-015 | Release readiness requires every non-live offline, process, native-binary, installer, and release row to pass; remaining pending rows may only be explicitly optional live-provider claims. | All | release | Closed procedure: (1) parse checklist tables; (2) group IDs by phase; (3) require PASS for every offline, process, native-binary, installer, and release row except REL-015 itself; (4) list remaining PENDING/FAIL rows; (5) compare live pending rows with disclosed support claims. | Every required non-live prerequisite is PASS; no non-live FAIL/PENDING remains; any remaining PENDING is a disclosed live-provider/plugin row and has no support claim. | PENDING |  |
